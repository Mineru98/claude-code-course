# 소프트웨어 개발과 제품 기획을 위한 실무 용어 백과

이 문서는 제품 요구사항 문서(PRD, Product Requirements Document)만으로는 실제 소프트웨어를 만들기 어렵다는 문제의식에서 출발한다. PRD는 “무엇을 만들 것인가”와 “왜 만들어야 하는가”를 설명하는 중심 문서이지만, 실제 개발 현장에서는 그것만으로 화면을 설계하고, 데이터를 정의하고, API를 합의하고, 테스트를 만들고, 서버를 배포하고, 장애를 운영할 수 없다. 그래서 제품과 시스템을 생산 가능한 수준으로 만들려면 정보 구조, 사용자 흐름, 화면 명세, 정책, 데이터 모델, API 계약, 테스트 시나리오, 인프라 운영 같은 여러 문서와 개념을 연결해서 이해해야 한다. 이 문서는 비전공자가 처음부터 따라갈 수 있도록 전문가들이 너무 당연하다고 여겨 생략하는 단어까지 풀어서 설명하는 데 목적이 있다.

용어를 외우는 것만으로는 실무 능력이 생기지 않는다. 실무에서 용어는 대화의 압축 도구이자 책임 범위를 나누는 표지판이다. 예를 들어 “이 화면의 빈 상태(Empty State)를 정의해야 합니다”라는 말은 단순히 빈 화면을 예쁘게 꾸미자는 뜻이 아니라, 사용자가 아직 데이터가 없을 때 무엇을 보고, 어떤 행동을 할 수 있고, 시스템은 어떤 데이터를 조회했으며, 해당 상태가 정상인지 오류인지까지 정해야 한다는 뜻이다. 같은 방식으로 “API 명세가 필요합니다”라는 말은 개발자가 임의로 주소를 정하자는 뜻이 아니라, 프론트엔드와 백엔드가 서로 주고받는 데이터의 형태, 오류 처리, 인증, 버전 관리, 보안 조건, 테스트 기준을 계약처럼 고정하자는 뜻이다. 이 문서는 그런 압축된 말들을 다시 길게 풀어 설명한다.

## 1. 정보 구조와 사용자 흐름

### 정보 구조(Information Architecture)

정보 구조(Information Architecture)는 사용자가 제품 안에서 정보를 어떻게 찾고, 이해하고, 이동하는지를 설계하는 체계다. 웹사이트나 앱을 처음 보는 사람은 내부 데이터베이스나 개발 구조를 알지 못하고, 화면에 드러난 메뉴, 탭, 검색, 카테고리, 제목, 버튼 이름을 통해 “이 서비스가 무엇을 할 수 있게 해 주는지”를 추측한다. 정보 구조라는 말은 도서관학, 건축, 웹 디자인의 영향을 함께 받았다. 도서관에서 책을 분류하고 서가를 배치하지 않으면 원하는 책을 찾기 어려운 것처럼, 디지털 제품도 기능과 콘텐츠가 아무리 많아도 사용자가 머릿속으로 이해할 수 있는 구조가 없으면 사용할 수 없다. 그래서 정보 구조는 단순한 메뉴 목록이 아니라 사용자의 인지 구조와 조직의 데이터 구조 사이를 중재하는 설계 작업이다.

정보 구조는 사이트맵(Sitemap), 내비게이션(Navigation), 콘텐츠 분류(Content Taxonomy), 사용자 여정(User Journey), 화면 흐름(Screen Flow)과 밀접하게 연결된다. 초보자는 흔히 정보 구조를 “메뉴 이름 정하기” 정도로 오해하지만, 실제로는 어떤 정보가 최상위에 올라와야 하는지, 어떤 정보는 상세 화면으로 숨겨도 되는지, 어떤 기능은 같은 그룹으로 묶어야 하는지, 검색으로 찾게 할지 탐색으로 찾게 할지까지 결정한다. 예를 들어 회사 내부 결재 시스템에서 “휴가 신청”을 인사 메뉴 밑에 둘지, 근태 메뉴 밑에 둘지, 개인 업무 바로가기 영역에 둘지는 단순한 취향 문제가 아니다. 사용자가 휴가를 어떤 맥락에서 떠올리는지, 조직이 휴가를 어떤 데이터로 관리하는지, 관리자가 어떤 보고서를 확인해야 하는지에 따라 달라진다.

실무에서는 정보 구조가 불명확할 때 회의가 끝없이 늘어난다. 기획자는 “이 기능을 어디에 넣어야 할지” 고민하고, 디자이너는 화면 배치를 반복해서 바꾸고, 개발자는 권한과 라우팅 구조를 다시 짠다. 경험 많은 실무자는 정보 구조를 먼저 정리하지 않은 채 화면부터 그리면 나중에 화면 수가 늘어나고, 같은 데이터가 여러 곳에서 다르게 보이며, 사용자가 현재 위치를 잃는다는 것을 안다. 반대로 지나치게 완벽한 정보 구조를 처음부터 만들려는 것도 위험하다. 실제 제품은 출시 후 사용자 행동 데이터, 조직 정책 변화, 신규 기능 추가에 따라 구조가 바뀐다. 좋은 정보 구조는 영원히 고정된 정답이 아니라, 현재 제품 목표와 사용자 과업에 맞게 충분히 명확하고 확장 가능한 구조다.

### 사이트맵(Sitemap)

사이트맵(Sitemap)은 제품이나 웹사이트 안에 어떤 주요 화면, 페이지, 메뉴, 하위 메뉴가 있는지를 계층 구조로 보여 주는 문서다. 원래 웹의 사이트맵은 검색 엔진이 웹페이지를 잘 수집하도록 돕는 기술적 파일을 의미하기도 했지만, 제품 기획과 UX 설계에서는 사용자가 보게 될 화면 구조를 나무 모양으로 정리한 설계 산출물을 뜻하는 경우가 많다. 사이트맵은 “우리 제품에 어떤 공간이 존재하는가”를 보여 준다. 건물로 비유하면 층별 안내도에 가깝고, 소설로 비유하면 목차에 가깝다. 사용자가 실제로 어떤 순서로 움직이는지는 사용자 흐름(User Flow)이 설명하고, 사이트맵은 그 흐름이 지나갈 수 있는 장소들의 전체 목록을 설명한다.

사이트맵은 정보 구조의 결과물이면서 동시에 화면 기획의 출발점이다. 예를 들어 쇼핑몰을 설계할 때 “홈, 상품 목록, 상품 상세, 장바구니, 주문서, 결제 완료, 마이페이지, 주문 내역, 배송 조회, 고객센터” 같은 화면을 먼저 나열하지 않으면, 화면별 요구사항이나 API 목록도 빠진다. 초보자가 자주 하는 실수는 사이트맵을 너무 화면 이름 중심으로만 만들고, 권한별 차이나 상태별 차이를 빠뜨리는 것이다. 같은 “주문 상세”라도 구매자, 판매자, 관리자, 고객센터 직원이 보는 정보와 가능한 행동이 다를 수 있다. 이런 경우 사이트맵에는 하나의 화면으로 보이지만 기능 명세에서는 역할별 변형 화면이나 권한별 표시 조건을 따로 관리해야 한다.

실무에서는 사이트맵이 범위 관리 도구로 자주 쓰인다. 프로젝트 초반에 사이트맵이 합의되면 “이번 1차 출시에는 고객센터 FAQ는 넣지만 1:1 문의는 제외한다”처럼 화면 단위로 범위를 조정할 수 있다. 다만 사이트맵만 보고 개발 공수를 정확히 판단하기는 어렵다. 단순한 화면처럼 보여도 결제, 정산, 권한, 외부 연동이 포함되면 훨씬 복잡해진다. 그래서 사이트맵은 반드시 기능 목록(Feature List), 데이터 모델(Data Model), API 목록(API List), 정책 문서(Policy Document)와 함께 봐야 한다. 사이트맵은 전체 지도를 주지만, 각 도로의 공사 난이도까지 알려 주지는 않는다.

### 내비게이션(Navigation)

내비게이션(Navigation)은 사용자가 제품 안에서 위치를 파악하고 다른 화면으로 이동할 수 있게 해 주는 구조와 UI 요소를 말한다. 상단 메뉴, 사이드바, 하단 탭, 빵부스러기 경로(Breadcrumb), 뒤로 가기, 검색, 바로가기 링크가 모두 내비게이션에 포함된다. 내비게이션이라는 개념은 물리적 공간에서 길을 찾는 행위에서 왔다. 사용자는 디지털 제품 안에서도 “내가 지금 어디에 있는지”, “어디로 갈 수 있는지”, “방금 전 화면으로 돌아갈 수 있는지”, “목표까지 얼마나 남았는지”를 알고 싶어 한다. 내비게이션은 이런 방향 감각을 제공한다.

내비게이션은 정보 구조와 사용자 흐름 사이에 놓인다. 정보 구조가 전체 분류 체계라면, 내비게이션은 그 구조를 실제 화면에서 이동 가능한 장치로 구현한 것이다. 예를 들어 정보 구조상 “계정 설정”과 “결제 설정”이 모두 설정 영역에 속한다고 해도, 실제 내비게이션에서는 자주 쓰는 “결제수단 관리”를 독립 메뉴로 빼는 것이 더 좋을 수 있다. 초보자는 메뉴가 많으면 친절하다고 생각하기 쉽지만, 메뉴가 너무 많으면 사용자는 오히려 중요한 행동을 찾지 못한다. 반대로 메뉴를 지나치게 숨기면 화면은 깔끔해져도 사용자는 기능이 존재한다는 사실을 알지 못한다. 좋은 내비게이션은 단순히 적은 메뉴가 아니라, 사용 빈도와 중요도, 권한, 작업 맥락에 맞게 드러나는 메뉴다.

실무에서 내비게이션은 제품의 성장과 함께 가장 많이 흔들리는 영역 중 하나다. 초기에는 기능이 적어서 상단 메뉴 몇 개로 충분하지만, 기능이 늘어나면 사이드바, 그룹 메뉴, 검색형 명령 팔레트, 즐겨찾기, 최근 사용 항목 같은 장치가 필요해진다. 기업용 관리자 도구에서는 사용자가 반복적으로 작업하기 때문에 화면 전환 횟수와 위치 기억이 중요하고, 소비자 앱에서는 첫 사용자가 길을 잃지 않도록 진입 경로와 뒤로 가기 동작이 중요하다. 내비게이션을 정할 때는 디자인만 보지 말고 브라우저 뒤로 가기, 모바일 하단 탭, 권한 없는 메뉴의 노출 여부, URL 구조, 접근성, 키보드 이동까지 함께 고려해야 한다.

### 사용자 흐름(User Flow)

사용자 흐름(User Flow)은 사용자가 특정 목표를 달성하기 위해 어떤 화면과 행동을 순서대로 거치는지를 나타낸다. 예를 들어 “회원가입하기”라는 목표를 달성하려면 사용자는 가입 버튼을 누르고, 약관에 동의하고, 이메일을 입력하고, 인증 코드를 받고, 비밀번호를 만들고, 프로필 정보를 입력하고, 완료 화면을 본다. 이 순서를 다이어그램이나 문장으로 정리한 것이 사용자 흐름이다. 사용자 흐름은 과거 업무 프로세스 모델링, 서비스 디자인의 고객 여정 지도, 소프트웨어 공학의 유스케이스 흐름에서 영향을 받았다. 제품 개발에서는 “사용자가 실제로 어떤 길을 걸어가는가”를 합의하기 위해 사용한다.

사용자 흐름은 사이트맵과 다르다. 사이트맵이 가능한 장소의 목록이라면, 사용자 흐름은 특정 목적을 가진 이동 경로다. 같은 사이트맵 안에서도 신규 사용자의 가입 흐름, 기존 사용자의 로그인 흐름, 비밀번호를 잊어버린 사용자의 복구 흐름, 관리자의 승인 흐름은 서로 다르다. 초보자가 자주 놓치는 것은 정상 흐름(Happy Path)만 그리고 예외 흐름(Exception Flow)을 빼먹는 것이다. 예를 들어 이메일 인증 코드를 잘못 입력하면 어떻게 되는지, 이미 가입된 이메일이면 어떤 메시지를 보여 주는지, 네트워크 오류가 나면 입력값은 유지되는지, 사용자가 중간에 뒤로 가면 동의 상태는 저장되는지 같은 문제가 실제 제품에서는 매우 중요하다.

실무에서 사용자 흐름은 개발자, 디자이너, QA가 함께 보는 핵심 자료다. 디자이너는 흐름을 보고 화면 간 연결과 버튼 위치를 설계하고, 개발자는 흐름을 보고 라우팅, 상태 저장, API 호출 순서를 정하고, QA는 흐름을 보고 테스트 시나리오를 만든다. 좋은 사용자 흐름 문서는 “A 화면에서 B 버튼을 누르면 C 화면으로 이동한다”만 쓰지 않고, 어떤 조건에서 이동이 허용되는지, 실패하면 어디에 머무는지, 사용자가 취소하면 어떤 상태로 돌아가는지까지 설명한다. 경험 많은 실무자는 흐름을 그릴 때 “사용자가 여기서 멈추면?”, “권한이 없으면?”, “데이터가 이미 있으면?”, “모바일 앱에서 백 버튼을 누르면?” 같은 질문을 계속 던진다.

### 고객 여정 지도(Customer Journey Map)

고객 여정 지도(Customer Journey Map)는 사용자가 제품을 알게 되는 순간부터 실제 사용, 문제 해결, 재방문, 이탈까지 이어지는 전체 경험을 시간순으로 정리한 문서다. 사용자 흐름이 제품 안의 구체적 행동 경로에 집중한다면, 고객 여정 지도는 제품 밖의 맥락까지 포함한다. 예를 들어 사용자가 광고를 보고 앱을 설치하고, 첫 로그인에서 막히고, 고객센터에 문의하고, 며칠 뒤 다시 알림을 받고, 결제를 고민하고, 결국 구독을 해지하는 과정 전체가 고객 여정이다. 이 개념은 서비스 디자인과 마케팅에서 널리 발전했으며, 고객 접점(Touchpoint)과 감정 변화, 문제점(Pain Point)을 함께 보는 것이 특징이다.

고객 여정 지도는 정보 구조, 사용자 흐름, 정책, 운영 프로세스와 연결된다. 예를 들어 사용자가 환불을 요청하는 여정에는 앱의 환불 신청 화면뿐 아니라 결제 정책, 고객센터 대응, 관리자 승인, PG사 처리, 이메일 알림, 회계 정산이 모두 연결된다. 초보자는 고객 여정 지도를 예쁜 포스터처럼 만들고 실제 의사결정에는 쓰지 않는 실수를 한다. 하지만 실무에서 중요한 것은 각 단계에서 사용자의 목표, 두려움, 질문, 장애물, 시스템 책임자를 명확히 하는 것이다. 고객 여정 지도가 “가입 단계에서 사용자가 불안함을 느낌”이라고만 쓰면 별 도움이 되지 않는다. “사업자등록번호 입력 단계에서 왜 필요한지 설명이 없어 이탈률이 높다”처럼 구체적이어야 개선으로 이어진다.

실무에서 고객 여정 지도는 부서 간 책임 경계를 드러내는 데 유용하다. 제품팀은 화면을 개선할 수 있지만, 고객센터 응답 지연은 운영팀의 문제일 수 있고, 결제 실패는 외부 결제사의 문제일 수 있으며, 배송 지연은 물류 정책의 문제일 수 있다. 고객 입장에서는 이 모든 것이 하나의 서비스 경험이지만, 회사 내부에서는 여러 팀으로 나뉘어 있다. 고객 여정 지도는 이런 분산된 책임을 한눈에 보이게 해 “우리 팀 일이 아니다”라는 말을 줄이는 데 도움을 준다. 다만 고객 여정 지도를 만들 때 실제 사용자 조사나 고객센터 데이터 없이 내부자의 상상만으로 작성하면 위험하다. 그 경우 지도는 사용자의 현실이 아니라 회사의 희망사항이 된다.

### 유스케이스(Use Case)

유스케이스(Use Case)는 특정 행위자(Actor)가 시스템을 사용해 달성하려는 목표와 그 상호작용을 설명하는 방식이다. 소프트웨어 공학에서 유스케이스는 객체지향 분석과 UML(Unified Modeling Language)의 역사 속에서 널리 쓰였으며, “시스템이 사용자에게 어떤 가치를 제공하는가”를 기능 중심으로 정리하기 위해 만들어졌다. 예를 들어 “회원이 주문을 취소한다”, “관리자가 상품을 승인한다”, “배송 담당자가 송장 번호를 등록한다”는 각각 유스케이스가 될 수 있다. 유스케이스는 화면 자체가 아니라 시스템과 사용자 사이의 목적 있는 대화를 설명한다.

유스케이스는 사용자 스토리(User Story), 기능 명세(Functional Specification), 권한 모델(Permission Model), 테스트 케이스(Test Case)와 연결된다. 사용자 스토리가 “누가, 무엇을, 왜 원하는가”를 짧게 표현한다면, 유스케이스는 그 목표를 이루기 위한 전제 조건, 기본 흐름, 대안 흐름, 예외 흐름, 사후 조건을 더 자세히 설명한다. 초보자는 유스케이스를 버튼 클릭 목록으로 착각하기 쉽다. 하지만 “사용자가 결제 버튼을 누른다”는 유스케이스가 아니라 흐름의 한 단계다. 유스케이스의 목표는 “사용자가 상품을 구매한다”이며, 그 안에 결제 버튼 클릭, 결제 승인, 주문 생성, 재고 차감, 알림 발송 같은 단계가 포함된다.

실무에서는 유스케이스를 너무 형식적으로 쓰면 팀이 읽지 않는다. 반대로 너무 느슨하게 쓰면 개발과 테스트의 기준이 되지 못한다. 좋은 유스케이스는 행위자, 목표, 시작 조건, 성공 조건, 실패 조건을 분명히 한다. 예를 들어 “관리자가 쿠폰을 발급한다”라는 유스케이스에서 누가 관리자인지, 쿠폰 발급 권한은 어떤 역할에 있는지, 이미 만료된 쿠폰을 복사할 수 있는지, 발급 후 수정 가능한 항목은 무엇인지, 발급 대상자가 10만 명이면 비동기 처리해야 하는지까지 실제 시스템 설계와 연결된다. 경험 많은 실무자는 유스케이스를 통해 화면에 보이지 않는 업무 규칙과 시스템 책임을 끄집어낸다.

### 사용자 스토리(User Story)

사용자 스토리(User Story)는 애자일 개발에서 많이 쓰이는 요구사항 표현 방식으로, 보통 “나는 어떤 사용자로서, 어떤 목적을 위해, 어떤 기능을 원한다”라는 형태를 가진다. 영어권에서는 “As a [role], I want [goal], so that [benefit]”라는 문장 구조가 유명하다. 이 형식은 개발팀이 기능을 기술적 작업이 아니라 사용자 가치 중심으로 이해하도록 돕기 위해 등장했다. 예를 들어 “회원으로서 주문 내역을 확인하고 싶다. 그래야 내가 구매한 상품의 배송 상태를 알 수 있다”는 사용자 스토리다.

사용자 스토리는 PRD, 백로그(Backlog), 수용 기준(Acceptance Criteria), 테스트 케이스와 연결된다. 초보자가 자주 하는 실수는 모든 요구사항을 사용자 스토리 문장으로만 쓰면 충분하다고 생각하는 것이다. 사용자 스토리는 의도를 잘 보여 주지만, 그 자체만으로는 화면, 데이터, API, 정책, 오류 처리를 다 담지 못한다. “회원으로서 배송지를 수정하고 싶다”라는 문장은 좋지만, 주문이 이미 배송 중이면 수정 가능한지, 기본 배송지와 주문 배송지가 어떻게 다른지, 해외 배송지는 허용되는지, 주소 검증은 어떻게 하는지, 수정 이력은 남기는지까지는 별도 명세가 필요하다.

실무에서 사용자 스토리는 대화의 시작점으로 쓰는 것이 좋다. 애자일에서 사용자 스토리는 원래 “카드, 대화, 확인”이라는 방식으로 이해된다. 짧은 카드에는 핵심 목표를 적고, 팀은 대화를 통해 세부사항을 합의하며, 수용 기준으로 완료 여부를 확인한다. 따라서 사용자 스토리를 계약서처럼 완벽한 문서로 오해하면 안 된다. 사용자 스토리가 좋은지 판단할 때는 독립적이고, 협상 가능하고, 가치가 있고, 추정 가능하고, 작고, 테스트 가능해야 한다는 INVEST 기준을 참고하기도 한다. 하지만 이 기준도 절대 규칙은 아니며, 조직의 문서 문화와 개발 방식에 맞게 조정해야 한다.

### 페르소나(Persona)

페르소나(Persona)는 제품을 사용할 대표적인 사용자 유형을 구체적인 인물처럼 묘사한 모델이다. “30대 직장인 여성”처럼 단순 인구통계가 아니라, 목표, 행동 패턴, 업무 환경, 불편함, 기술 숙련도, 의사결정 기준까지 포함한다. 페르소나는 마케팅, 사용자 조사, UX 디자인에서 발전한 개념이며, 팀이 추상적인 “사용자” 대신 구체적인 사람을 떠올리며 의사결정하도록 돕는다. 예를 들어 회계 SaaS의 페르소나는 “월말마다 세금계산서 누락을 걱정하는 5년 차 경리 담당자”처럼 업무 맥락이 살아 있어야 한다.

페르소나는 고객 여정 지도, 사용자 흐름, 기능 우선순위, 화면 문구와 연결된다. 같은 기능이라도 페르소나에 따라 설계가 달라진다. 초보 사용자는 안내와 오류 설명이 중요하고, 숙련 사용자는 빠른 입력과 단축키가 중요하다. 초보자가 자주 하는 실수는 페르소나를 상상으로만 만들거나, 사진과 이름만 붙인 장식 자료로 만드는 것이다. 페르소나는 실제 사용자 인터뷰, 고객센터 기록, 사용 데이터, 영업 피드백 같은 근거를 가져야 실무 가치가 있다. 근거 없는 페르소나는 팀의 편견을 그럴듯하게 포장할 수 있다.

실무에서는 페르소나가 의사결정 기준으로 작동해야 한다. “이 버튼 문구가 좋나요?”라는 질문에 단순 취향으로 답하는 대신 “우리 핵심 페르소나는 회계 용어에 익숙하지 않으니 ‘정산 확정’보다 ‘이번 달 받을 금액 확인’이 낫다”처럼 판단할 수 있어야 한다. 하지만 페르소나를 지나치게 고정된 캐릭터로 취급하는 것도 위험하다. 실제 사용자는 한 사람이 여러 역할을 수행하거나 상황에 따라 초보자와 숙련자처럼 행동할 수 있다. 특히 기업용 제품에서는 구매자, 관리자, 실사용자, 승인자가 서로 다를 수 있으므로 페르소나도 역할과 권한을 구분해야 한다.

### 터치포인트(Touchpoint)

터치포인트(Touchpoint)는 사용자가 회사나 제품과 접촉하는 모든 지점을 말한다. 앱 화면, 웹사이트, 이메일, 문자, 푸시 알림, 고객센터 전화, 영업 미팅, 결제 영수증, 배송 알림, 도움말 문서까지 모두 터치포인트가 될 수 있다. 이 용어는 서비스 디자인과 고객 경험 관리에서 중요하게 쓰이며, 디지털 제품이 화면 안에서만 존재하지 않는다는 사실을 강조한다. 사용자는 회사 내부 조직도를 알지 못하므로, 앱에서 본 안내와 이메일에서 받은 안내가 다르면 하나의 서비스가 모순된다고 느낀다.

터치포인트는 고객 여정 지도, 운영 정책, 알림 정책, 콘텐츠 전략과 연결된다. 초보자는 제품 기획을 화면 중심으로만 생각해 이메일 문구, 문자 발송 시점, 고객센터 답변 템플릿을 나중에 처리할 부수 작업으로 여긴다. 그러나 실제 운영에서는 이런 터치포인트가 사용자 신뢰를 크게 좌우한다. 예를 들어 결제는 성공했는데 영수증 이메일이 오지 않으면 사용자는 결제가 제대로 되었는지 불안해한다. 환불 신청 화면은 정상인데 고객센터가 다른 기준으로 안내하면 분쟁이 생긴다.

실무에서 터치포인트를 설계할 때는 메시지의 일관성, 발송 조건, 책임 팀, 실패 처리, 개인정보 노출을 함께 봐야 한다. 푸시 알림 하나도 단순 문구가 아니라 사용자가 수신 동의했는지, 야간 발송 제한이 있는지, 같은 메시지를 여러 번 받지 않는지, 알림을 눌렀을 때 어느 화면으로 이동하는지, 이미 처리된 주문에 대한 알림을 뒤늦게 누르면 어떤 화면을 보여 줄지까지 정해야 한다. 경험 많은 실무자는 터치포인트가 늘어날수록 운영 비용과 품질 관리 난이도가 올라간다는 것을 안다.

## 2. 화면 와이어프레이밍과 기능 명세

### 화면 기획서(Wireframe and Functional Specification)

화면 기획서(Wireframe and Functional Specification)는 특정 화면이 어떤 목적을 가지며, 어떤 요소를 보여 주고, 사용자가 어떤 행동을 할 수 있고, 그 행동에 시스템이 어떻게 반응해야 하는지를 설명하는 문서다. 한국 실무에서는 “와이어프레임”, “스토리보드”, “화면설계서”, “기능 명세서”라는 말이 섞여 쓰이지만, 엄밀히 보면 와이어프레임은 화면의 구조적 배치에 가깝고 기능 명세는 동작 조건과 규칙에 가깝다. 화면 기획서는 이 둘을 함께 담아 개발자, 디자이너, QA가 같은 화면을 같은 방식으로 이해하게 하는 역할을 한다.

화면 기획서는 PRD, 정보 구조, 사용자 흐름, 디자인 시안, API 명세, 테스트 케이스와 연결된다. PRD가 왜 이 기능이 필요한지를 말한다면, 화면 기획서는 “이 화면에서 사용자가 실제로 무엇을 보고 누르는가”를 말한다. 초보자는 화면 기획서를 그림으로만 생각해 버튼과 입력칸의 위치만 그리는 경우가 많다. 그러나 실무에서 더 중요한 것은 각 요소의 표시 조건, 입력 제한, 기본값, 정렬 기준, 오류 메시지, 로딩 상태, 빈 상태, 권한별 노출 여부, 저장 후 이동 경로다. 예를 들어 “저장 버튼” 하나도 입력값이 유효하지 않을 때 비활성화할지, 누른 뒤 로딩 표시를 할지, 중복 클릭을 막을지, 저장 실패 시 사용자가 입력한 내용을 유지할지 정해야 한다.

실무에서 화면 기획서는 너무 자세해도 문제고 너무 추상적이어도 문제다. 모든 픽셀과 색상을 기획서에 고정하면 디자이너의 역할이 줄고 변경 비용이 커진다. 반대로 “목록을 보여 준다” 정도로만 쓰면 개발자는 무엇을 기준으로 목록을 정렬하고 어떤 항목을 표시해야 하는지 알 수 없다. 좋은 화면 기획서는 시각 디자인이 아니라 기능적 의사결정에 집중한다. 화면에 보이는 데이터가 어디서 오는지, 사용자가 조작하면 어떤 상태가 바뀌는지, 실패할 때 어떤 메시지가 나오는지, 완료 기준이 무엇인지가 명확해야 한다.

### 와이어프레임(Wireframe)

와이어프레임(Wireframe)은 화면의 시각적 완성도를 낮춘 상태에서 정보 배치, 기능 위치, 화면 구조를 표현한 설계 그림이다. 원래 와이어프레임이라는 말은 3D 그래픽이나 설계에서 뼈대 구조를 선으로 표현하는 데서 왔고, UX에서는 색상과 세부 디자인보다 레이아웃과 정보 우선순위를 빠르게 검토하기 위한 도구로 자리 잡았다. 와이어프레임은 최종 디자인이 아니라 토론용 골격이다. 그래서 흑백 박스, 간단한 텍스트, 버튼 자리 표시자만으로도 충분할 때가 많다.

와이어프레임은 정보 구조, 화면 기획서, 프로토타입(Prototype), 디자인 시안(Mockup)과 연결된다. 초보자는 와이어프레임을 예쁘게 만드는 데 시간을 많이 쓰거나, 반대로 너무 대충 그려 중요한 기능 조건을 놓치기도 한다. 와이어프레임의 목적은 예쁨이 아니라 구조적 합의다. 예를 들어 대시보드 화면에서 매출 그래프를 상단에 둘지, 경고 목록을 상단에 둘지, 필터를 왼쪽에 둘지 상단에 둘지 같은 결정은 사용자 업무 우선순위를 반영한다. 색상과 그림자를 정하기 전에도 이런 구조는 검토할 수 있다.

실무에서는 와이어프레임의 정밀도가 프로젝트 단계에 따라 달라진다. 초기 아이디어 단계에서는 손그림이나 간단한 도형으로 충분하고, 개발 직전에는 요소 이름, 데이터 예시, 상태별 변형을 포함한 상세 와이어프레임이 필요하다. 중요한 예외는 기존 디자인 시스템이 강한 조직이다. 이미 표준 테이블, 버튼, 모달, 필터 컴포넌트가 있다면 와이어프레임은 실제 컴포넌트 구조를 따라야 개발과 디자인이 빨라진다. 반대로 없는 컴포넌트를 와이어프레임에서 아무렇지 않게 그리면 개발 공수와 디자인 시스템 확장 비용이 생긴다.

### 스토리보드(Storyboard)

스토리보드(Storyboard)는 화면의 순서와 전환, 사용자 행동, 시스템 반응을 장면 단위로 설명하는 문서다. 영화와 광고 제작에서 장면 흐름을 미리 그려 보는 방식에서 온 말이며, 한국 IT 실무에서는 화면설계서 전체를 “스토리보드”라고 부르는 경우도 많다. 좁은 의미의 스토리보드는 한 화면의 배치보다 여러 화면이 어떤 순서로 이어지는지에 초점을 둔다. 예를 들어 로그인 실패, 비밀번호 재설정, 인증 메일 발송, 완료 화면까지 이어지는 흐름을 장면처럼 보여 준다.

스토리보드는 사용자 흐름, 와이어프레임, 기능 명세, 테스트 시나리오와 연결된다. 초보자는 스토리보드를 단순히 화면 캡처를 나열한 문서로 만들기 쉽다. 그러나 실무적으로 가치 있는 스토리보드는 각 화면에서 사용자의 의도, 시스템 상태, 이동 조건, 예외 상황을 같이 설명한다. “다음 버튼을 누르면 다음 화면으로 이동”이라는 문장만 반복하면 스토리보드의 역할을 하지 못한다. 왜 다음으로 갈 수 있는지, 어떤 데이터가 저장되는지, 어떤 검증을 통과해야 하는지, 중간에 이탈하면 무엇이 남는지를 설명해야 한다.

스토리보드는 특히 외주 개발, SI 프로젝트, 여러 부서가 함께 일하는 프로젝트에서 중요하게 쓰인다. 말로만 설명한 흐름은 사람마다 다르게 이해하지만, 화면 순서와 동작을 문서로 고정하면 오해를 줄일 수 있다. 다만 스토리보드를 너무 완벽한 계약서처럼 만들면 변경이 어려워진다. 애자일 조직에서는 스토리보드 대신 피그마 프로토타입, 이슈 티켓, 수용 기준, API 명세를 조합해서 쓰기도 한다. 중요한 것은 문서 이름이 아니라 “화면과 흐름과 규칙이 충분히 합의되었는가”다.

### 기능 명세(Functional Specification)

기능 명세(Functional Specification)는 시스템이 어떤 기능을 제공해야 하며, 각 기능이 어떤 조건에서 어떻게 동작해야 하는지를 구체적으로 설명하는 문서다. 기능 명세는 “사용자가 버튼을 누르면 저장된다”보다 훨씬 넓은 범위를 가진다. 입력값 검증, 권한 확인, 데이터 생성과 수정, 알림 발송, 외부 시스템 연동, 오류 처리, 이력 기록, 성능 조건까지 기능의 일부가 될 수 있다. 기능 명세는 소프트웨어 공학의 요구사항 명세에서 발전했고, 개발자와 QA가 구현과 검증의 기준으로 삼는다.

기능 명세는 화면 기획서, API 명세, 데이터 모델, 비기능 요구사항(Non-functional Requirements), 테스트 케이스와 연결된다. 초보자는 기능 명세를 “기능 목록”과 혼동한다. “회원가입, 로그인, 상품 조회, 주문하기”는 기능 목록이지 기능 명세가 아니다. 기능 명세는 “회원가입 시 이메일은 필수이며, 이미 가입된 이메일은 사용할 수 없고, 비밀번호는 최소 8자 이상이며, 가입 완료 후 기본 역할은 일반 회원이고, 인증 메일은 10분 동안 유효하다”처럼 동작 규칙을 담아야 한다.

실무에서 기능 명세가 약하면 개발자는 추측으로 구현하고, QA는 무엇이 버그인지 판단하지 못하며, 운영팀은 고객 문의에 일관되게 답하지 못한다. 특히 중요한 것은 “하지 말아야 할 동작”과 “예외 조건”이다. 예를 들어 쿠폰 기능에서 정상 발급만 명세하고 중복 발급, 부분 취소, 환불 후 쿠폰 복구, 만료 직전 사용, 시간대 차이, 관리자 수동 지급을 명세하지 않으면 출시 후 문제가 생긴다. 경험 많은 실무자는 기능 명세를 읽을 때 “상태가 바뀌는 순간”, “돈이나 권한이 움직이는 순간”, “외부 시스템에 요청하는 순간”을 특히 주의해서 본다.

### 수용 기준(Acceptance Criteria)

수용 기준(Acceptance Criteria)은 어떤 기능이나 사용자 스토리가 완료되었다고 받아들일 수 있는 조건을 명확히 적은 기준이다. 애자일 개발에서 사용자 스토리와 함께 자주 쓰이며, 개발자, 기획자, QA, 이해관계자가 “이 정도면 끝난 것인가”를 같은 기준으로 판단하게 한다. 수용 기준은 기능 명세보다 더 검증 중심적이다. 기능 명세가 시스템 동작을 설명한다면, 수용 기준은 그 동작이 충족되었는지 확인할 수 있는 문장으로 표현된다.

수용 기준은 테스트 케이스, 완료 정의(Definition of Done), QA 시나리오, 배포 승인과 연결된다. 초보자는 수용 기준을 “잘 동작해야 한다”, “사용자가 쉽게 이해할 수 있어야 한다”처럼 모호하게 쓰는 실수를 한다. 이런 문장은 검증할 수 없기 때문에 기준이 되지 못한다. 좋은 수용 기준은 관찰 가능하고 판단 가능해야 한다. 예를 들어 “이메일 형식이 아닌 값을 입력하고 가입 버튼을 누르면, 사용자는 이메일 입력칸 아래에 ‘올바른 이메일 주소를 입력해 주세요’라는 오류 메시지를 보고 가입 요청은 서버로 전송되지 않는다”처럼 결과가 구체적이어야 한다.

실무에서는 수용 기준을 너무 세세하게 써서 기능 명세와 중복되기도 하고, 너무 적게 써서 QA가 다시 해석해야 하기도 한다. 좋은 균형은 핵심 성공 조건과 주요 예외 조건을 중심으로 잡는 것이다. 특히 돈, 개인정보, 권한, 법적 동의, 재고, 배송처럼 위험이 큰 기능은 수용 기준을 더 촘촘하게 써야 한다. 경험 많은 팀은 Given-When-Then 형식을 사용하기도 한다. “주어진 조건에서, 사용자가 어떤 행동을 하면, 시스템은 어떤 결과를 보여 준다”라는 구조는 테스트로 옮기기 쉽기 때문이다. 다만 형식보다 중요한 것은 기준이 실제로 테스트 가능하다는 점이다.

### 상태(State)

상태(State)는 화면, 데이터, 사용자, 주문, 시스템 등이 특정 시점에 가지고 있는 조건이나 값을 말한다. 소프트웨어는 정적인 문서가 아니라 상태가 계속 변하는 시스템이다. 사용자는 로그인 전 상태와 로그인 후 상태가 다르고, 주문은 결제 대기, 결제 완료, 배송 준비, 배송 중, 배송 완료, 취소, 환불 같은 상태를 가진다. 상태라는 개념은 컴퓨터 과학의 상태 기계(State Machine), 업무 프로세스 관리, UI 설계 모두에서 중요하다. 제품을 정확히 설계하려면 “무엇이 보이는가”뿐 아니라 “지금 이것은 어떤 상태인가”를 알아야 한다.

상태는 상태값(Status), 상태 전이(State Transition), 권한, 화면 표시 조건, API 응답, 테스트 케이스와 연결된다. 초보자는 상태를 단순 텍스트 라벨로 생각하기 쉽지만, 상태는 가능한 행동을 결정한다. 예를 들어 주문 상태가 “배송 중”이면 배송지 수정 버튼은 사라지거나 비활성화되어야 하고, “결제 실패” 상태면 재결제 버튼이 보여야 한다. 상태가 명확하지 않으면 같은 주문에 대해 화면마다 다른 버튼이 보이거나, 이미 취소된 주문을 다시 취소하는 오류가 발생한다.

실무에서는 상태 설계가 어렵고 중요하다. 상태가 너무 적으면 현실을 표현하지 못하고, 너무 많으면 운영과 개발이 복잡해진다. 예를 들어 “취소”라는 한 상태로 충분해 보이지만, 실제로는 사용자가 취소 요청만 한 상태, 관리자가 승인한 상태, 결제사가 환불 처리 중인 상태, 환불이 완료된 상태, 환불 실패 상태가 필요할 수 있다. 특히 외부 시스템과 연결된 업무에서는 내부 상태와 외부 상태가 일시적으로 불일치할 수 있다. 경험 많은 실무자는 상태 전이표를 만들어 어떤 상태에서 어떤 상태로 이동할 수 있는지, 누가 그 전이를 일으키는지, 실패하면 어떤 상태로 남는지를 확인한다.

### 빈 상태(Empty State)

빈 상태(Empty State)는 화면이 정상적으로 로드되었지만 보여 줄 데이터가 없을 때 사용자에게 표시하는 상태다. 예를 들어 아직 주문 내역이 없는 사용자에게 “주문 내역이 없습니다”라고 보여 주거나, 검색 결과가 없을 때 다른 검색어를 제안하는 화면이 빈 상태다. 빈 상태는 오류 상태(Error State)와 다르다. 오류 상태는 시스템이 데이터를 가져오지 못했거나 문제가 발생한 것이고, 빈 상태는 데이터가 없는 것이 정상인 경우다. 이 구분이 중요한 이유는 사용자에게 주는 메시지와 다음 행동이 달라지기 때문이다.

빈 상태는 화면 명세, 콘텐츠 전략, 온보딩, 사용자 흐름, QA 테스트와 연결된다. 초보자는 데이터가 있을 때의 화면만 설계하고 데이터가 없을 때를 잊는다. 그러나 실제 제품에서는 신규 사용자, 첫 사용, 필터 조건이 너무 좁은 경우, 권한 때문에 볼 수 있는 데이터가 없는 경우 등 빈 상태가 자주 발생한다. 빈 상태를 정의하지 않으면 개발자는 빈 테이블만 보여 주거나, 로딩이 끝났는지 알 수 없는 화면을 만들 수 있다. 사용자는 시스템이 고장난 것인지, 아직 데이터가 없는 것인지, 자신이 무엇을 해야 하는지 알지 못한다.

실무에서 좋은 빈 상태는 상황을 설명하고 다음 행동을 제안한다. “데이터가 없습니다”보다 “아직 등록된 상품이 없습니다. 첫 상품을 등록하면 고객에게 노출됩니다”가 더 유용하다. 하지만 모든 빈 상태에 큰 일러스트와 긴 설명을 넣는 것도 좋지 않다. 업무용 도구에서는 간결하고 반복 사용에 방해되지 않는 빈 상태가 더 적합할 수 있다. 또한 빈 상태에는 여러 종류가 있다. 전체 데이터가 없는 최초 빈 상태, 검색 결과가 없는 빈 상태, 필터 결과가 없는 빈 상태, 권한이 없어 비어 보이는 상태는 서로 다른 문구와 행동을 가져야 한다.

### 로딩 상태(Loading State)

로딩 상태(Loading State)는 시스템이 데이터를 가져오거나 작업을 처리하는 동안 사용자에게 보여 주는 중간 상태다. 과거 웹이 단순 문서 중심일 때는 페이지 전체가 새로고침되며 로딩이 드러났지만, 현대 웹과 앱은 화면 일부만 비동기로 갱신되기 때문에 사용자가 “지금 기다려야 하는지”, “버튼이 눌렸는지”, “시스템이 멈췄는지”를 알 수 있도록 로딩 상태를 명시해야 한다. 스피너, 스켈레톤 화면(Skeleton Screen), 진행률 바, 버튼 내부 로딩 표시, 비활성화 상태가 모두 로딩 표현이다.

로딩 상태는 API 호출, 성능, 사용자 경험, 중복 요청 방지, 오류 처리와 연결된다. 초보자는 로딩을 단순한 장식으로 생각하지만, 실무에서는 데이터 정합성과도 관련된다. 사용자가 저장 버튼을 누른 뒤 아무 표시가 없으면 여러 번 클릭할 수 있고, 그 결과 중복 주문이나 중복 신청이 발생할 수 있다. 반대로 로딩 상태가 끝나지 않으면 사용자는 시스템이 멈췄다고 생각한다. 어떤 작업은 즉시 끝나고, 어떤 작업은 오래 걸리며, 어떤 작업은 백그라운드에서 처리되어야 한다. 로딩 상태는 이런 차이를 사용자에게 알려 주는 장치다.

실무에서 로딩 상태를 설계할 때는 범위가 중요하다. 화면 전체를 막아야 하는 로딩인지, 특정 버튼만 로딩 처리하면 되는지, 목록 일부만 갱신하면 되는지 결정해야 한다. 예를 들어 결제 처리 중에는 사용자가 다른 행동을 하지 못하게 전체 화면을 막는 것이 안전할 수 있지만, 댓글 목록을 불러오는 동안 전체 페이지를 막는 것은 불편하다. 또한 로딩이 너무 짧게 나타났다 사라지면 화면이 깜빡이는 느낌을 줄 수 있고, 너무 오래 지속되면 대체 행동이나 오류 안내가 필요하다. 경험 많은 실무자는 로딩, 성공, 실패, 빈 상태를 하나의 세트로 설계한다.

### 오류 상태(Error State)

오류 상태(Error State)는 사용자의 입력, 시스템 처리, 네트워크, 권한, 외부 연동, 서버 내부 문제 등으로 인해 요청한 작업이 정상적으로 완료되지 못했을 때의 상태다. 오류는 제품 품질을 드러내는 중요한 순간이다. 사용자는 성공할 때보다 실패할 때 시스템을 더 자세히 판단한다. 오류 메시지가 모호하거나 책임을 사용자에게만 돌리면 신뢰가 떨어진다. 오류 상태는 전통적인 소프트웨어 공학의 예외 처리(Exception Handling)와 UX의 사용자 안내가 만나는 지점이다.

오류 상태는 입력 검증(Validation), API 오류 코드, 로깅(Logging), 모니터링(Monitoring), 고객센터 대응, 테스트 케이스와 연결된다. 초보자는 오류 메시지를 “오류가 발생했습니다” 하나로 처리하려는 경향이 있다. 하지만 실무에서는 오류의 원인과 사용자가 할 수 있는 행동에 따라 메시지가 달라져야 한다. 비밀번호가 틀린 오류, 네트워크 연결 오류, 서버 점검 오류, 권한 없음, 이미 처리된 요청, 외부 결제사 오류는 모두 다르다. 특히 보안상 이유로 너무 자세히 말하면 안 되는 오류도 있다. 로그인 실패 시 “이메일은 맞지만 비밀번호가 틀렸습니다”라고 알려 주면 공격자가 계정 존재 여부를 추측할 수 있다.

실무에서 오류 상태를 잘 설계하려면 사용자 메시지와 내부 로그를 분리해야 한다. 사용자에게는 이해 가능한 문장과 다음 행동을 제공하고, 개발자에게는 오류 코드, 요청 ID, 스택 트레이스, 외부 응답값 같은 진단 정보를 남겨야 한다. 또한 오류는 재시도 가능한 오류와 재시도해도 소용없는 오류로 나뉜다. 일시적 네트워크 오류는 다시 시도 버튼이 필요하지만, 권한 없는 요청은 다시 시도보다 권한 요청 안내가 필요하다. 경험 많은 팀은 오류 코드 체계를 만들고, QA는 주요 오류 상태를 실제로 발생시켜 화면과 로그가 기대대로 동작하는지 확인한다.

### 입력 검증(Validation)

입력 검증(Validation)은 사용자가 입력한 값이 시스템이 요구하는 형식, 범위, 규칙을 만족하는지 확인하는 과정이다. 이메일 형식, 비밀번호 길이, 필수 입력 여부, 숫자 범위, 날짜 순서, 중복 여부, 사업자등록번호 유효성, 파일 크기 제한 등이 입력 검증에 포함된다. 입력 검증은 오래된 웹 폼 시대부터 중요했지만, 현대 제품에서는 보안, 데이터 품질, 사용자 경험과 모두 연결된다. 잘못된 데이터가 시스템에 들어가면 나중에 수정 비용이 커지고, 보안 취약점이 생기며, 운영팀이 수작업으로 정리해야 할 수도 있다.

입력 검증은 프론트엔드 검증, 백엔드 검증, 데이터베이스 제약조건, 오류 메시지, 테스트 케이스와 연결된다. 초보자는 화면에서 검증하면 충분하다고 생각하기 쉽다. 하지만 프론트엔드 검증은 사용자 편의를 위한 것이고, 보안과 데이터 정합성을 위해서는 서버에서도 반드시 검증해야 한다. 사용자는 브라우저 개발자 도구나 직접 API 호출로 프론트엔드 검증을 우회할 수 있다. 또 프론트엔드와 백엔드의 검증 규칙이 다르면 화면에서는 통과했는데 서버에서 실패하는 불쾌한 경험이 생긴다.

실무에서 입력 검증은 언제 오류를 보여 줄지도 중요하다. 사용자가 한 글자 입력할 때마다 빨간 오류를 보여 주면 부담스럽고, 제출 후에만 오류를 보여 주면 뒤늦게 많이 고쳐야 한다. 필드에서 벗어났을 때 검증할지, 제출 시 검증할지, 서버 중복 확인은 언제 할지 결정해야 한다. 또한 입력값을 자동으로 보정할지 거부할지도 판단해야 한다. 전화번호의 하이픈을 자동 제거하는 것은 편할 수 있지만, 이름이나 주소를 임의로 바꾸면 문제가 된다. 경험 많은 실무자는 검증 규칙을 문서화할 때 “허용값”, “거부값”, “자동 변환”, “오류 문구”, “서버 검증 여부”를 함께 적는다.

## 3. 비즈니스와 운영 정책

### 비즈니스 규칙(Business Rule)

비즈니스 규칙(Business Rule)은 회사의 사업 방식, 법적 조건, 운영 판단, 수익 모델, 리스크 관리에 따라 시스템이 따라야 하는 업무 규칙이다. 예를 들어 “가입 후 7일 이내에는 무료 체험을 제공한다”, “이미 배송이 시작된 주문은 사용자가 직접 취소할 수 없다”, “관리자 승인 전 상품은 고객에게 노출되지 않는다”, “쿠폰은 중복 사용할 수 없다” 같은 문장이 비즈니스 규칙이다. 비즈니스 규칙은 기술적 한계에서 나온 것이 아니라 사업과 운영의 의사결정에서 나온다.

비즈니스 규칙은 기능 명세, 정책 문서, 데이터 모델, 권한, 테스트 케이스, 운영 매뉴얼과 연결된다. 초보자는 비즈니스 규칙을 화면 문구나 개발 로직과 혼동한다. 하지만 비즈니스 규칙은 화면이 바뀌어도 유지될 수 있고, 여러 기능에 걸쳐 반복 적용된다. 예를 들어 “미성년자는 특정 상품을 구매할 수 없다”는 규칙은 상품 상세, 장바구니, 결제, 주문 검수, 고객센터 처리 모두에 영향을 준다. 이 규칙을 한 화면에만 적어 두면 다른 경로에서 누락된다.

실무에서 비즈니스 규칙을 명확히 하지 않으면 개발자는 자기 상식으로 판단하고, 운영자는 나중에 예외 처리를 요구하며, 고객은 일관되지 않은 경험을 하게 된다. 특히 할인, 환불, 정산, 권한, 승인, 법적 동의, 개인정보 보존 같은 영역은 비즈니스 규칙이 복잡하다. 경험 많은 실무자는 규칙을 쓸 때 “적용 대상”, “적용 조건”, “예외”, “우선순위”, “변경 권한”, “시행일”을 확인한다. 비즈니스 규칙은 시간이 지나며 바뀌기 때문에 코드에만 숨겨 두면 위험하다. 문서와 시스템이 함께 관리되어야 한다.

### 운영 정책(Operational Policy)

운영 정책(Operational Policy)은 제품이 출시된 후 실제 업무를 어떻게 처리할지에 대한 원칙과 절차를 말한다. 비즈니스 규칙이 시스템의 판단 기준이라면, 운영 정책은 사람과 조직이 개입하는 처리 방식까지 포함한다. 예를 들어 “환불 요청은 접수 후 영업일 기준 3일 이내 처리한다”, “고객센터는 본인 확인 후 배송지를 수정할 수 있다”, “관리자 수동 정산은 팀장 승인 후 실행한다” 같은 내용이 운영 정책이다. 운영 정책은 제품이 현실 세계의 조직과 만나면서 필요해진다.

운영 정책은 관리자 화면, 권한 모델, 감사 로그(Audit Log), 고객센터 스크립트, SLA(Service Level Agreement), 법무 검토와 연결된다. 초보자는 제품을 만들면 운영은 자연스럽게 따라온다고 생각하기 쉽다. 그러나 실제로는 운영 정책이 없으면 문의가 쌓이고, 담당자마다 다르게 처리하며, 고객 간 형평성 문제가 생긴다. 예를 들어 환불 화면을 만들었는데 환불 승인 권한, 부분 환불 기준, 악성 사용자 대응, 외부 결제 취소 실패 시 절차가 없으면 운영팀은 매번 개발자에게 물어봐야 한다.

실무에서 운영 정책은 지나치게 이상적이면 지켜지지 않는다. “모든 문의를 즉시 처리한다”는 좋은 말이지만 인력과 시스템이 뒷받침되지 않으면 정책이 아니다. 좋은 운영 정책은 업무량, 담당자 권한, 시스템 기능, 법적 책임, 고객 기대를 함께 고려한다. 또한 운영 정책은 관리자 기능의 요구사항이 된다. 운영자가 주문을 수정해야 한다면 수정 화면, 권한 확인, 수정 사유 입력, 변경 이력, 알림 발송이 필요하다. 경험 많은 실무자는 사용자용 화면만이 아니라 운영자용 화면과 운영 절차를 함께 설계한다.

### 정책(Policy)

정책(Policy)은 특정 상황에서 조직이나 시스템이 어떤 원칙에 따라 판단하고 행동할지를 정한 규칙 묶음이다. 비즈니스 정책, 운영 정책, 보안 정책, 개인정보 처리 정책, 알림 정책, 환불 정책처럼 다양한 종류가 있다. 정책이라는 말은 법률과 행정에서 오래전부터 쓰였고, 소프트웨어 제품에서는 사람의 판단을 일관된 시스템 동작으로 바꾸기 위해 사용된다. 정책은 “무엇을 허용하고 금지하는가”, “어떤 조건에서 예외를 인정하는가”, “누가 결정권을 가지는가”를 설명한다.

정책은 기능 명세보다 상위에 있을 때가 많다. 예를 들어 “환불 정책”은 환불 신청 화면, 관리자 승인 화면, 결제 취소 API, 고객센터 답변, 회계 처리, 약관 문구에 모두 영향을 준다. 초보자는 정책을 약관 같은 법적 문서로만 생각하지만, 실무에서는 제품 동작을 결정하는 매우 실용적인 문서다. “동일 사용자는 첫 구매 쿠폰을 한 번만 받을 수 있다”는 정책은 마케팅 문구가 아니라 데이터베이스 제약, 쿠폰 발급 로직, 부정 사용 탐지와 연결된다.

실무에서 정책 문서를 쓸 때 가장 중요한 것은 경계 조건이다. 정책은 평범한 경우보다 애매한 경우에서 가치가 드러난다. 예를 들어 “구매 후 7일 이내 환불 가능”이라는 정책에서 7일은 결제 시각 기준인지 배송 완료 기준인지, 시간대는 어떻게 처리하는지, 주말과 공휴일은 포함되는지, 일부 사용한 서비스는 어떻게 하는지, 프로모션 상품은 예외인지 정해야 한다. 경험 많은 실무자는 정책을 만들 때 실제 고객 문의와 분쟁 사례를 상상하고, 문구가 시스템으로 구현 가능한지 개발자와 확인한다.

### 권한(Role and Permission)

권한(Role and Permission)은 사용자가 시스템 안에서 어떤 데이터에 접근하고 어떤 행동을 할 수 있는지를 제한하는 체계다. 역할(Role)은 사용자 유형의 묶음이고, 권한(Permission)은 구체적인 행동 가능 여부다. 예를 들어 “관리자”는 역할이고, “상품 승인 가능”, “주문 환불 가능”, “회원 개인정보 조회 가능”은 권한이다. 권한 체계는 보안, 조직 책임, 개인정보 보호, 운영 사고 방지에서 매우 중요하다. 초기 시스템은 단순히 일반 사용자와 관리자만 구분해도 되지만, 조직이 커지면 더 세밀한 권한이 필요해진다.

권한은 인증(Authentication), 인가(Authorization), 화면 표시 조건, API 보안, 감사 로그, 운영 정책과 연결된다. 초보자는 로그인만 되면 권한 문제가 해결된다고 생각하기 쉽다. 하지만 로그인은 “누구인지 확인”하는 것이고, 권한은 “무엇을 할 수 있는지 판단”하는 것이다. 관리자로 로그인했더라도 모든 관리자에게 환불 권한을 줄 필요는 없다. 또한 화면에서 버튼을 숨기는 것만으로 권한 처리가 끝나지 않는다. 사용자가 직접 API를 호출할 수 있으므로 서버에서도 권한을 반드시 확인해야 한다.

실무에서 권한 설계는 조직 구조와 자주 충돌한다. 어떤 회사는 직급 기준으로 권한을 주고 싶어 하고, 어떤 회사는 업무 담당 기준으로 권한을 주고 싶어 한다. 권한이 너무 넓으면 사고 위험이 커지고, 너무 좁으면 업무가 막힌다. 또한 임시 권한, 대리 승인, 퇴사자 권한 회수, 외주 인력 접근, 개인정보 마스킹 같은 현실적 문제가 있다. 경험 많은 실무자는 권한을 설계할 때 “읽기, 생성, 수정, 삭제, 승인, 내보내기”를 분리하고, 민감한 행동에는 사유 입력과 감사 로그를 남기도록 한다.

### 승인 워크플로(Approval Workflow)

승인 워크플로(Approval Workflow)는 어떤 요청이나 변경이 즉시 실행되지 않고, 정해진 승인자나 조건을 거쳐 확정되는 절차다. 휴가 신청, 비용 정산, 상품 등록 승인, 게시글 검수, 환불 승인, 권한 요청이 대표적인 예다. 승인 워크플로는 기업 업무 시스템에서 오래전부터 존재했고, 디지털 제품에서는 이를 화면, 상태, 알림, 권한, 이력으로 구현한다. 승인 워크플로는 단순히 “승인 버튼”을 추가하는 것이 아니라 업무 책임과 통제 구조를 시스템에 반영하는 일이다.

승인 워크플로는 상태 전이, 권한, 알림, SLA, 감사 로그, 예외 처리와 연결된다. 초보자는 승인 절차를 “요청, 승인, 반려” 세 가지로만 생각하기 쉽다. 실제 업무에서는 임시 저장, 제출, 회수, 수정 요청, 1차 승인, 2차 승인, 대리 승인, 자동 승인, 만료, 취소, 반려 후 재신청 같은 상태가 필요할 수 있다. 승인자가 휴가 중이면 어떻게 할지, 승인 대상자가 조직 이동을 하면 어떻게 할지, 이미 승인된 건을 취소할 수 있는지 같은 문제도 생긴다.

실무에서 승인 워크플로는 운영 효율과 내부 통제 사이의 균형이다. 모든 것을 승인받게 하면 안전해 보이지만 업무가 느려지고, 아무나 처리하게 하면 사고가 난다. 좋은 워크플로는 위험도가 높은 행동에만 필요한 승인 단계를 두고, 반복적이고 낮은 위험의 행동은 자동화한다. 또한 승인 이력은 나중에 분쟁이나 감사에서 중요한 증거가 된다. 누가 언제 어떤 사유로 승인했는지, 승인 당시의 데이터가 무엇이었는지 남겨야 한다. 단순히 현재 상태만 저장하면 나중에 왜 그런 결정이 내려졌는지 알 수 없다.

### 예외 처리(Exception Handling)

예외 처리(Exception Handling)는 정상적인 규칙이나 흐름으로 처리하기 어려운 상황을 어떻게 다룰지 정하는 것이다. 개발에서는 프로그램 실행 중 발생하는 오류를 처리하는 의미로 쓰이고, 제품과 운영에서는 정책의 일반 규칙에서 벗어난 특수 사례를 처리하는 의미로도 쓰인다. 예를 들어 결제는 성공했지만 주문 생성이 실패한 경우, 배송 중 상품이 분실된 경우, 사용자가 법적 환불 기간은 지났지만 회사 실수로 피해를 본 경우가 예외다. 예외 처리는 실제 서비스 운영의 핵심이다.

예외 처리는 정책, 운영 매뉴얼, 관리자 기능, 고객센터 대응, 로그, 보상 정책과 연결된다. 초보자는 예외를 “나중에 운영자가 알아서 처리”하면 된다고 생각하기 쉽다. 그러나 운영자가 처리하려면 시스템에 필요한 기능이 있어야 한다. 수동 환불, 상태 변경, 쿠폰 보상, 주문 재생성, 알림 재발송, 외부 요청 재시도 같은 도구가 없으면 개발자가 데이터베이스를 직접 수정해야 하고, 이는 큰 위험을 만든다.

실무에서 모든 예외를 미리 완벽하게 설계할 수는 없다. 하지만 돈, 개인정보, 법적 의무, 외부 연동, 대량 처리와 관련된 예외는 반드시 주요 시나리오를 준비해야 한다. 예외 처리를 설계할 때는 누가 판단하는지, 어떤 증거를 확인하는지, 어떤 권한이 필요한지, 처리 후 기록은 어떻게 남기는지, 고객에게 어떤 메시지를 보내는지 정해야 한다. 경험 많은 실무자는 예외 처리를 단순 “오류 대응”이 아니라 운영 안정성을 위한 필수 기능으로 본다.

### 감사 로그(Audit Log)

감사 로그(Audit Log)는 시스템에서 중요한 행동이 언제, 누구에 의해, 어떤 대상에 대해, 어떤 값에서 어떤 값으로 변경되었는지를 추적하기 위해 남기는 기록이다. 감사라는 말은 회계와 보안 영역에서 왔으며, 나중에 문제가 생겼을 때 사실관계를 확인하기 위한 증거를 의미한다. 일반 로그가 시스템 진단을 위한 기술적 기록이라면, 감사 로그는 책임 추적과 규정 준수에 더 가깝다. 예를 들어 관리자가 회원 등급을 변경했거나, 환불을 승인했거나, 개인정보를 조회했거나, 권한을 부여했다면 감사 로그를 남겨야 한다.

감사 로그는 권한, 운영 정책, 보안, 개인정보 보호, 장애 조사, 관리자 화면과 연결된다. 초보자는 로그를 개발자가 디버깅할 때만 보는 것으로 생각하기 쉽다. 그러나 감사 로그는 고객 분쟁, 내부 사고, 법적 요구, 보안 사고 조사에서 매우 중요하다. “누가 환불 처리했나요?”, “왜 배송지가 바뀌었나요?”, “이 사용자의 개인정보를 누가 조회했나요?” 같은 질문에 답하려면 감사 로그가 필요하다. 단순히 현재 데이터만 보면 과거 변경 과정을 알 수 없다.

실무에서 감사 로그는 조작되면 안 되고, 너무 민감한 정보를 그대로 담아도 안 된다. 비밀번호, 카드번호, 주민등록번호 같은 정보는 로그에 남기면 보안 사고가 된다. 또한 로그가 너무 많으면 저장 비용과 검색 비용이 커지고, 너무 적으면 조사에 쓸 수 없다. 경험 많은 실무자는 감사 로그에 행위자, 대상, 행동, 이전 값과 이후 값, 시각, IP나 요청 ID, 사유를 남기되, 민감정보는 마스킹하거나 제외한다. 또한 감사 로그는 일반 운영자가 삭제하거나 수정할 수 없게 해야 한다.

## 4. API 명세와 데이터 모델링

### API(Application Programming Interface)

API(Application Programming Interface)는 서로 다른 소프트웨어 구성요소가 정해진 방식으로 요청과 응답을 주고받기 위한 인터페이스다. 제품 개발에서 API라고 하면 보통 프론트엔드 화면과 백엔드 서버, 모바일 앱과 서버, 내부 시스템과 외부 서비스가 통신하는 규칙을 의미한다. API는 식당의 주문 방식에 비유할 수 있다. 손님은 주방 내부를 알 필요 없이 메뉴판에 있는 형식대로 주문하고, 주방은 약속된 결과를 제공한다. 소프트웨어에서도 호출자는 내부 구현을 몰라도 정해진 주소, 메서드, 요청 데이터, 응답 데이터를 사용해 기능을 이용한다.

API는 화면 명세, 데이터 모델, 인증, 오류 처리, 테스트, 외부 연동과 연결된다. 초보자는 API를 단순히 “서버 주소”라고 생각하기 쉽지만, 실제 API 명세에는 HTTP 메서드, URL 경로, 요청 헤더, 요청 본문, 쿼리 파라미터, 응답 형식, 오류 코드, 인증 방식, 권한 조건, 속도 제한, 버전 정보가 포함된다. 예를 들어 “주문 목록 API”는 `/orders`라는 주소만으로 충분하지 않다. 어떤 사용자의 주문을 보여 주는지, 페이지네이션은 어떻게 하는지, 취소된 주문도 포함하는지, 날짜 필터는 어떤 형식인지, 응답의 주문 상태값은 무엇인지 정해야 한다.

실무에서 API는 팀 간 계약이다. 프론트엔드 개발자는 API 응답을 믿고 화면을 만들고, 백엔드 개발자는 명세를 지키며 서버를 구현하고, QA는 명세를 기준으로 테스트한다. API 명세가 자주 바뀌면 여러 팀의 작업이 흔들린다. 그래서 OpenAPI Specification 같은 표준으로 문서화하거나, API 목(Mock)을 만들어 프론트엔드가 백엔드 완성 전에도 개발할 수 있게 한다. 경험 많은 실무자는 API를 설계할 때 현재 화면만 보지 않고 향후 확장, 버전 호환성, 보안, 오류 처리, 데이터 일관성을 함께 고려한다.

### 엔드포인트(Endpoint)

엔드포인트(Endpoint)는 API에서 특정 기능이나 리소스에 접근하기 위한 주소와 호출 지점을 말한다. 예를 들어 `GET /users/{id}`는 특정 사용자를 조회하는 엔드포인트이고, `POST /orders`는 주문을 생성하는 엔드포인트다. 엔드포인트라는 말은 네트워크 통신에서 연결의 끝점을 의미하며, 웹 API에서는 클라이언트가 요청을 보내는 구체적 URL 경로를 가리킨다. 엔드포인트는 API의 문처럼 작동한다. 어떤 문은 조회만 허용하고, 어떤 문은 데이터를 생성하거나 수정한다.

엔드포인트는 HTTP 메서드, 리소스(Resource), 라우팅(Routing), 권한, API 문서와 연결된다. 초보자는 모든 동작을 하나의 엔드포인트에 몰아넣거나, 반대로 너무 많은 엔드포인트를 만드는 실수를 한다. 예를 들어 `/orderAction` 하나에 생성, 취소, 환불, 수정 기능을 모두 넣으면 요청 본문의 `action` 값에 따라 동작이 달라져 이해와 테스트가 어려워진다. 반대로 아주 작은 내부 단계마다 엔드포인트를 만들면 클라이언트가 복잡한 순서를 직접 관리해야 한다. 좋은 엔드포인트 설계는 리소스와 행동의 책임을 명확히 나눈다.

실무에서 엔드포인트 이름은 오래 남는다. 화면 문구는 바꾸기 쉽지만, 공개 API나 모바일 앱이 사용하는 엔드포인트는 함부로 바꾸기 어렵다. 이미 배포된 앱이 예전 엔드포인트를 호출하고 있을 수 있기 때문이다. 그래서 엔드포인트 설계에서는 명확하고 일관된 네이밍이 중요하다. 또한 권한이 필요한 엔드포인트는 서버에서 반드시 인증과 인가를 확인해야 한다. 화면에서 버튼이 없다고 해서 엔드포인트가 안전한 것은 아니다. 경험 많은 개발자는 엔드포인트마다 “누가 호출할 수 있는가”, “몇 번까지 호출할 수 있는가”, “실패하면 어떤 응답을 주는가”를 확인한다.

### 요청(Request)과 응답(Response)

요청(Request)과 응답(Response)은 API 통신의 기본 단위다. 클라이언트가 서버에 보내는 메시지가 요청이고, 서버가 처리 결과로 돌려주는 메시지가 응답이다. 웹의 HTTP 구조는 원래 문서를 가져오기 위한 요청과 응답 모델에서 발전했지만, 현대 API에서는 데이터 조회, 생성, 수정, 삭제, 파일 업로드, 인증, 결제 등 거의 모든 상호작용이 요청과 응답으로 표현된다. 요청에는 메서드, 주소, 헤더, 파라미터, 본문이 들어갈 수 있고, 응답에는 상태 코드, 헤더, 본문이 들어간다.

요청과 응답은 API 명세, 데이터 모델, 오류 코드, 보안, 테스트와 연결된다. 초보자는 요청값과 응답값을 예시 몇 개로만 생각하기 쉽다. 그러나 실무에서는 필수값과 선택값, 기본값, 허용 범위, 데이터 타입, 배열의 최대 길이, 날짜 형식, null 허용 여부, 빈 문자열 허용 여부를 명확히 해야 한다. 응답도 성공일 때만 정의하면 부족하다. 입력값 오류, 인증 실패, 권한 없음, 데이터 없음, 서버 오류, 외부 시스템 오류의 응답 구조를 정해야 클라이언트가 적절히 처리할 수 있다.

실무에서 요청과 응답 설계는 프론트엔드와 백엔드의 생산성을 크게 좌우한다. 응답이 화면에 필요한 데이터를 충분히 제공하지 않으면 프론트엔드는 여러 API를 추가로 호출해야 하고, 너무 많은 데이터를 제공하면 성능과 보안 문제가 생긴다. 예를 들어 회원 목록 화면에는 회원의 이름과 상태만 필요할 수 있는데, 응답에 주소와 전화번호까지 포함하면 불필요한 개인정보 노출이 된다. 경험 많은 실무자는 화면 요구사항을 보면서도 데이터 최소화, 확장 가능성, 캐싱, 페이지네이션, 오류 처리까지 함께 본다.

### HTTP 메서드(HTTP Method)

HTTP 메서드(HTTP Method)는 클라이언트가 서버에 어떤 종류의 작업을 요청하는지 나타내는 동사다. 대표적으로 `GET`은 조회, `POST`는 생성이나 처리 요청, `PUT`은 전체 교체, `PATCH`는 일부 수정, `DELETE`는 삭제에 사용된다. 이 개념은 월드 와이드 웹의 HTTP 프로토콜에서 시작되었고, REST API 설계에서 리소스에 대한 행동을 일관되게 표현하는 핵심 요소가 되었다. 메서드는 API를 읽는 사람에게 이 호출이 데이터를 바꾸는지 아닌지 알려 주는 중요한 신호다.

HTTP 메서드는 엔드포인트, REST, 캐싱, 멱등성(Idempotency), 보안과 연결된다. 초보자는 모든 API를 `POST`로 만들어도 동작하니 괜찮다고 생각하기 쉽다. 하지만 `GET`과 `POST`의 의미를 지키지 않으면 브라우저 캐싱, 검색 엔진, 프록시, 모니터링, 보안 장비가 요청을 잘못 이해할 수 있다. 특히 `GET` 요청은 원칙적으로 서버 상태를 바꾸지 않아야 한다. `GET /deleteUser?id=1`처럼 조회 메서드로 삭제를 수행하면 링크 미리보기나 크롤러가 의도치 않게 데이터를 삭제할 위험이 있다.

실무에서 메서드 선택은 명확성과 안정성의 문제다. `PUT`과 `PATCH`의 차이를 엄격히 지키는 조직도 있고, 대부분 수정을 `PATCH`로 처리하는 조직도 있다. 중요한 것은 팀 내 일관성이다. 또한 결제나 주문 생성처럼 중복 요청이 위험한 API에서는 메서드뿐 아니라 멱등성 키(Idempotency Key) 같은 장치가 필요하다. 사용자가 저장 버튼을 두 번 누르거나 네트워크 재시도로 같은 요청이 반복될 수 있기 때문이다. 경험 많은 개발자는 “이 API가 재호출되면 어떤 일이 생기는가”를 항상 묻는다.

### 데이터 모델(Data Model)

데이터 모델(Data Model)은 시스템이 다루는 주요 개체(Entity), 속성(Attribute), 관계(Relationship), 제약조건(Constraint)을 구조화한 설계다. 예를 들어 쇼핑몰에는 회원, 상품, 주문, 주문상품, 결제, 배송, 쿠폰 같은 개체가 있고, 회원은 여러 주문을 가질 수 있으며, 주문은 여러 주문상품을 포함한다. 데이터 모델은 현실 세계의 업무 개념을 컴퓨터가 저장하고 처리할 수 있는 구조로 바꾸는 작업이다. 데이터베이스 설계, 도메인 모델링, 객체 모델링과 밀접하다.

데이터 모델은 ERD(Entity Relationship Diagram), API 응답, 화면 표시, 비즈니스 규칙, 데이터베이스 스키마와 연결된다. 초보자는 화면에 보이는 항목만 데이터 모델로 생각하기 쉽다. 그러나 화면에 보이지 않아도 시스템 운영에 필요한 데이터가 많다. 예를 들어 주문 화면에는 현재 상태만 보일 수 있지만, 실제로는 생성 시각, 수정 시각, 결제 승인 번호, 취소 사유, 환불 상태, 배송 추적 번호, 감사 이력 등이 필요할 수 있다. 반대로 화면에 보인다고 모두 저장해야 하는 것은 아니다. 계산으로 얻을 수 있는 값은 저장하지 않고 필요할 때 계산할 수도 있다.

실무에서 데이터 모델은 나중에 바꾸기 어려운 핵심 설계다. 잘못된 데이터 모델은 화면과 API를 계속 복잡하게 만든다. 예를 들어 주문과 결제를 하나의 테이블로 뭉치면 부분 결제, 재결제, 환불, 결제 실패 처리에서 문제가 생길 수 있다. 하지만 지나치게 이론적으로 쪼개면 개발 속도가 느려지고 조회가 복잡해진다. 경험 많은 실무자는 현재 요구사항과 예상되는 변화 사이에서 균형을 잡는다. 또한 데이터 모델을 만들 때는 이름을 신중히 정해야 한다. “상태”, “구분”, “타입” 같은 모호한 이름은 나중에 혼란을 만든다.

### 엔티티(Entity)

엔티티(Entity)는 시스템이 독립적으로 식별하고 관리해야 하는 대상이나 개념이다. 회원, 주문, 상품, 게시글, 결제, 조직, 프로젝트, 파일, 알림 같은 것이 엔티티가 될 수 있다. 엔티티라는 말은 데이터 모델링과 객체지향 설계에서 모두 쓰이며, 현실의 사물이나 업무 개념을 소프트웨어 안에서 하나의 단위로 다루기 위해 사용된다. 엔티티는 보통 고유 식별자(ID)를 가지며, 여러 속성과 관계를 가진다.

엔티티는 속성, 관계, 데이터베이스 테이블, 도메인 모델, API 리소스와 연결된다. 초보자는 화면의 카드 하나나 목록 행 하나를 바로 엔티티로 착각하기도 한다. 물론 화면에 보이는 목록 항목이 엔티티일 수 있지만, 화면 표현과 데이터 모델은 다를 수 있다. 예를 들어 주문 목록의 한 행에는 주문, 결제, 배송 정보가 합쳐져 보일 수 있다. 이때 화면 행은 하나지만 데이터 모델의 엔티티는 여러 개다. 반대로 하나의 엔티티가 여러 화면에서 다르게 표시될 수도 있다.

실무에서 엔티티를 잘못 잡으면 시스템이 업무 현실을 어색하게 표현하게 된다. 예를 들어 “사용자”와 “회원”과 “계정”을 구분하지 않으면 로그인 계정은 하나인데 여러 조직에 소속되는 경우, 탈퇴한 회원의 주문 이력을 보존해야 하는 경우, 같은 사람이 구매자와 판매자 역할을 동시에 하는 경우에 혼란이 생긴다. 경험 많은 설계자는 엔티티를 정할 때 “이 대상은 독립적으로 생성, 수정, 삭제, 조회되는가”, “고유한 생명주기가 있는가”, “다른 대상과 어떤 관계를 가지는가”, “법적 또는 운영상 보존해야 하는가”를 확인한다.

### 속성(Attribute)과 필드(Field)

속성(Attribute)과 필드(Field)는 엔티티가 가지고 있는 구체적인 데이터 항목을 말한다. 회원 엔티티에는 이름, 이메일, 전화번호, 가입일, 상태 같은 속성이 있을 수 있고, 주문 엔티티에는 주문번호, 주문일, 총금액, 배송지, 주문상태 같은 속성이 있을 수 있다. 데이터 모델링에서는 속성이라는 말을 많이 쓰고, 데이터베이스나 API 문서에서는 필드라는 말을 많이 쓴다. 둘은 문맥에 따라 거의 비슷하게 쓰이지만, 속성은 개념적 의미가 강하고 필드는 실제 데이터 구조의 칸이라는 느낌이 강하다.

속성과 필드는 데이터 타입(Data Type), 필수 여부, 기본값, 제약조건, 입력 검증, 화면 표시와 연결된다. 초보자는 속성 이름만 적으면 충분하다고 생각하기 쉽다. 하지만 실무에서는 각 필드가 문자열인지 숫자인지 날짜인지, null을 허용하는지, 최대 길이는 얼마인지, 사용자가 직접 입력하는지 시스템이 자동 생성하는지, 변경 가능한지, 개인정보인지, 검색 가능한지까지 정해야 한다. 예를 들어 “전화번호” 필드는 국가번호를 포함하는지, 하이픈을 저장하는지, 인증된 번호인지, 여러 개 저장할 수 있는지에 따라 설계가 달라진다.

실무에서 필드 하나의 애매함이 큰 문제를 만든다. “주소”를 하나의 문자열로 저장하면 처음에는 편하지만, 나중에 우편번호 검색, 지역별 배송비, 국가별 주소 형식, 상세주소 분리, 개인정보 마스킹에서 어려움이 생길 수 있다. 반대로 너무 세밀하게 나누면 입력과 관리가 복잡해진다. 또한 필드 이름은 화면 문구와 다를 수 있다. 화면에는 “받는 분”이라고 보여도 데이터 모델에서는 `recipientName`일 수 있다. 경험 많은 실무자는 필드 정의서에 의미, 타입, 예시, 필수 여부, 생성 주체, 수정 가능 여부, 보안 등급을 함께 적는다.

### 관계(Relationship)

관계(Relationship)는 데이터 모델에서 엔티티들이 서로 어떻게 연결되는지를 설명한다. 회원은 여러 주문을 가질 수 있고, 주문은 여러 상품을 포함할 수 있으며, 상품은 여러 카테고리에 속할 수 있다. 관계는 일대일(One-to-One), 일대다(One-to-Many), 다대다(Many-to-Many) 같은 형태로 표현된다. 관계 개념은 관계형 데이터베이스 이론에서 중요하게 발전했으며, 현실 세계의 연결성을 데이터 구조로 표현하기 위해 필요하다.

관계는 외래키(Foreign Key), 조인(Join), API 응답 구조, 권한, 삭제 정책과 연결된다. 초보자는 관계를 단순히 “서로 관련 있음”으로만 생각하지만, 실무에서는 관계의 방향, 필수 여부, 생명주기, 삭제 시 처리 방식이 중요하다. 예를 들어 주문은 반드시 회원이 있어야 생성되는지, 비회원 주문도 가능한지, 회원이 탈퇴하면 주문은 삭제되는지 보존되는지, 상품이 삭제되면 과거 주문의 상품명은 어떻게 되는지 정해야 한다.

실무에서 관계 설계는 데이터 정합성과 조회 성능에 큰 영향을 준다. 다대다 관계는 보통 중간 테이블을 만들어 표현한다. 예를 들어 상품과 카테고리는 여러 상품이 여러 카테고리에 속할 수 있으므로 상품카테고리 같은 연결 엔티티가 필요하다. 또한 현재 관계와 과거 이력을 구분해야 한다. 주문 당시의 상품 가격은 이후 상품 가격이 바뀌어도 변하면 안 되므로, 주문에는 당시 가격을 별도로 저장해야 한다. 경험 많은 설계자는 “현재 참조해도 되는 데이터”와 “거래 당시 스냅샷으로 보존해야 하는 데이터”를 구분한다.

### 스키마(Schema)

스키마(Schema)는 데이터의 구조, 형식, 제약조건을 정의한 틀이다. 데이터베이스 스키마는 테이블, 컬럼, 인덱스, 제약조건을 정의하고, API 스키마는 요청과 응답의 필드 구조와 타입을 정의한다. 스키마라는 말은 원래 구조나 계획을 뜻하며, 소프트웨어에서는 데이터가 어떤 모양이어야 하는지 기계와 사람이 모두 이해할 수 있게 표현하는 데 쓰인다. 스키마는 데이터의 문법과 의미를 어느 정도 고정한다.

스키마는 데이터 모델, 마이그레이션(Migration), 유효성 검증, API 문서, 타입 시스템과 연결된다. 초보자는 스키마를 개발자가 알아서 만드는 내부 구조로만 생각하기 쉽다. 그러나 스키마가 바뀌면 화면, API, 테스트, 운영 데이터, 외부 연동까지 영향을 받을 수 있다. 예를 들어 회원 테이블에서 전화번호를 필수로 바꾸면 기존 전화번호 없는 회원 데이터는 어떻게 할지, 가입 API는 어떻게 바뀌는지, 화면에는 어떤 안내가 필요한지, 개인정보 처리방침은 수정해야 하는지 검토해야 한다.

실무에서 스키마 변경은 신중하게 해야 한다. 새 필드를 추가하는 것은 비교적 쉽지만, 필드를 삭제하거나 타입을 바꾸거나 필수 조건을 강화하는 것은 위험하다. 이미 운영 데이터가 있기 때문이다. 마이그레이션을 통해 기존 데이터를 변환해야 하고, 배포 순서도 고려해야 한다. 프론트엔드가 새 필드를 보내기 전에 백엔드가 받을 준비가 되어 있어야 하며, 모바일 앱처럼 사용자가 오래된 버전을 계속 쓸 수 있는 경우 하위 호환성이 중요하다. 경험 많은 팀은 스키마 변경을 작은 단계로 나누고, 롤백 가능성을 검토한다.

### 정규화(Normalization)

정규화(Normalization)는 데이터베이스에서 중복을 줄이고 데이터의 일관성을 높이기 위해 데이터를 적절한 테이블로 나누는 설계 원칙이다. 관계형 데이터베이스 이론에서 발전한 개념으로, 같은 정보를 여러 곳에 저장하면 한 곳만 수정되고 다른 곳은 수정되지 않는 불일치가 발생하기 때문에 이를 방지하려는 목적이 있다. 예를 들어 고객 주소를 주문마다 모두 현재 주소로 참조하면 주소 변경 시 과거 주문의 배송지가 바뀌는 문제가 생길 수 있고, 반대로 필요한 곳에 중복 저장하지 않으면 과거 사실을 보존하지 못할 수도 있다.

정규화는 엔티티, 관계, 중복 데이터, 성능, 비정규화(Denormalization)와 연결된다. 초보자는 정규화를 무조건 많이 할수록 좋다고 생각하거나, 반대로 복잡하니 모두 한 테이블에 넣으려는 극단으로 가기 쉽다. 정규화의 목적은 이론적 아름다움이 아니라 데이터 무결성이다. 회원 정보, 상품 정보, 주문 정보를 적절히 분리하면 한 개념의 변경이 다른 개념을 부당하게 바꾸지 않는다. 하지만 조회 성능이나 이력 보존을 위해 의도적으로 일부 데이터를 중복 저장하는 경우도 있다.

실무에서 정규화와 비정규화는 균형 문제다. 운영성 데이터베이스에서는 정합성이 중요해 정규화가 유리하고, 분석용 데이터나 고성능 조회 화면에서는 비정규화가 유리할 수 있다. 예를 들어 주문 목록에서 매번 주문상품과 결제와 배송을 모두 조인하면 느릴 수 있어 요약 금액이나 대표 상품명을 주문 테이블에 저장할 수 있다. 하지만 이렇게 중복 저장한 값은 원본과 불일치할 수 있으므로 갱신 전략이 필요하다. 경험 많은 설계자는 “이 값의 진짜 원천은 어디인가”를 명확히 한다.

## 5. QA 테스트 시나리오와 테스트 케이스

### QA(Quality Assurance)

QA(Quality Assurance)는 제품이 요구사항과 품질 기준을 만족하는지 확인하고, 결함을 예방하며, 출시 가능한 상태인지 판단하는 활동이다. 한국어로는 품질 보증이라고 번역되지만, 단순히 버그를 찾는 일만 의미하지 않는다. QA는 요구사항 검토, 테스트 계획, 테스트 케이스 작성, 결함 보고, 회귀 테스트, 출시 승인, 품질 지표 관리까지 포함할 수 있다. 역사적으로 제조업의 품질 관리에서 온 개념이 소프트웨어 개발에 적용되면서, 완성품 검사뿐 아니라 개발 과정 전체의 품질 확보로 의미가 확장되었다.

QA는 수용 기준, 테스트 시나리오, 테스트 케이스, 버그 리포트, 배포, 운영 모니터링과 연결된다. 초보자는 QA를 개발이 끝난 뒤 마지막에 한 번 눌러 보는 작업으로 생각하기 쉽다. 그러나 늦게 발견된 결함일수록 수정 비용이 커진다. 요구사항이 애매할 때 QA가 초기에 질문하면 개발 전부터 문제를 줄일 수 있다. 예를 들어 “환불 가능 기간이 7일”이라고만 쓰여 있으면 QA는 기준 시각과 예외를 질문해야 한다. 이런 질문은 버그를 찾는 것이 아니라 버그가 생기기 전에 모호함을 제거하는 일이다.

실무에서 QA는 모든 것을 완벽히 테스트할 수 없다는 현실을 전제로 한다. 시간과 인력이 제한되어 있으므로 위험 기반 테스트(Risk-Based Testing)가 중요하다. 결제, 회원가입, 개인정보, 권한, 데이터 삭제, 대량 발송처럼 실패 비용이 큰 영역을 우선 검증해야 한다. 또한 QA는 개발자를 비난하는 역할이 아니라 제품의 신뢰성을 높이는 역할이다. 좋은 QA 리포트는 감정적 표현이 아니라 재현 조건, 기대 결과, 실제 결과, 환경, 증거를 명확히 제공한다.

### 테스트 시나리오(Test Scenario)

테스트 시나리오(Test Scenario)는 사용자의 목표나 업무 흐름 단위로 무엇을 검증할지 큰 줄기를 설명한 것이다. 예를 들어 “신규 사용자가 이메일로 가입하고 첫 주문을 완료한다”, “관리자가 환불 요청을 승인한다”, “권한 없는 사용자가 관리자 페이지에 접근한다”가 테스트 시나리오다. 시나리오는 이야기나 상황에 가깝고, 여러 테스트 케이스를 포함할 수 있다. 테스트 시나리오는 사용자 흐름과 유스케이스에서 파생되며, 실제 사용 맥락을 기준으로 품질을 확인하기 위해 존재한다.

테스트 시나리오는 테스트 케이스, 수용 기준, 사용자 흐름, 회귀 테스트와 연결된다. 초보자는 시나리오와 케이스를 구분하지 못하는 경우가 많다. 시나리오는 “무엇을 검증할 것인가”에 가깝고, 테스트 케이스는 “어떤 조건에서 어떤 절차로 실행하고 어떤 결과를 기대하는가”에 가깝다. 하나의 로그인 시나리오 안에는 정상 로그인, 비밀번호 오류, 미가입 이메일, 잠긴 계정, 2단계 인증 실패, 네트워크 오류 같은 여러 케이스가 들어갈 수 있다.

실무에서 테스트 시나리오는 QA 범위를 설명하는 데 유용하다. 프로젝트 이해관계자는 수백 개의 테스트 케이스를 모두 읽기 어렵지만, 주요 시나리오 목록을 보면 어떤 사용자 흐름을 검증하는지 알 수 있다. 좋은 시나리오는 실제 사용자의 목적과 업무 흐름을 반영한다. 단순히 화면별로 “버튼 클릭 테스트”만 나열하면 전체 흐름의 결함을 놓칠 수 있다. 경험 많은 QA는 정상 시나리오뿐 아니라 실패 시나리오, 권한 시나리오, 경계값 시나리오, 중단 후 재개 시나리오를 함께 설계한다.

### 테스트 케이스(Test Case)

테스트 케이스(Test Case)는 특정 조건에서 어떤 절차를 실행하고 어떤 결과가 나와야 하는지를 상세히 적은 검증 단위다. 테스트 케이스에는 보통 테스트 ID, 목적, 사전 조건, 테스트 데이터, 실행 단계, 기대 결과, 실제 결과, 통과 여부가 포함된다. 테스트 케이스는 수동 QA에서도 쓰이고 자동화 테스트에서도 개념적으로 중요하다. 테스트 케이스가 있어야 같은 기능을 여러 사람이 반복 검증해도 일관된 결과를 얻을 수 있다.

테스트 케이스는 수용 기준, 기능 명세, 입력 검증, 오류 상태, 버그 리포트와 연결된다. 초보자는 테스트 케이스를 “한번 해 볼 행동 목록”으로 작성하는 경우가 많다. 그러나 좋은 테스트 케이스는 판단 가능한 기대 결과를 가져야 한다. “회원가입을 한다”는 테스트 단계일 뿐이고, “가입 완료 후 사용자는 홈 화면으로 이동하며, 환영 이메일이 발송되고, 데이터베이스에 기본 역할이 일반 회원으로 저장된다”가 기대 결과다. 기대 결과가 모호하면 통과와 실패를 판단할 수 없다.

실무에서 테스트 케이스는 유지보수 비용이 든다. 화면과 기능이 바뀌면 테스트 케이스도 바뀌어야 한다. 그래서 모든 사소한 UI 행동을 지나치게 세분화하면 문서가 금방 낡는다. 반대로 핵심 흐름과 위험 영역의 테스트 케이스가 부족하면 출시 후 문제가 생긴다. 경험 많은 QA는 테스트 케이스의 상세 수준을 위험도에 맞춘다. 결제, 환불, 권한, 데이터 삭제는 매우 자세히 쓰고, 단순 문구 변경은 간단히 검증한다. 또한 테스트 데이터가 중요하다. 동일한 케이스라도 신규 사용자, 기존 사용자, 정지 사용자, 관리자 계정에 따라 결과가 달라진다.

### 정상 흐름(Happy Path)

정상 흐름(Happy Path)은 모든 조건이 기대대로 맞아떨어져 사용자가 목표를 문제없이 달성하는 기본 시나리오다. 예를 들어 사용자가 올바른 이메일과 비밀번호로 로그인하고, 상품 재고가 있으며, 결제가 승인되고, 주문이 정상 생성되는 과정이 정상 흐름이다. “Happy Path”라는 표현은 개발과 테스트에서 오래 쓰인 비공식적 용어로, 시스템이 가장 순조롭게 동작하는 길을 의미한다. 정상 흐름은 기능의 기본 가치를 확인하는 데 필요하다.

정상 흐름은 예외 흐름, 오류 흐름, 경계값 테스트, 사용자 흐름과 연결된다. 초보자는 정상 흐름을 완성하면 기능이 끝났다고 생각하기 쉽다. 그러나 실제 사용자는 잘못 입력하고, 중간에 나가고, 네트워크가 끊기고, 권한이 없고, 데이터가 없는 상태에서 제품을 사용한다. 정상 흐름만 테스트하면 출시 후 실전에서 많은 버그가 드러난다. 그래도 정상 흐름은 중요하다. 정상 흐름이 명확하지 않으면 예외 흐름도 설계할 기준이 없다.

실무에서 정상 흐름은 데모와 초기 개발의 기준으로 많이 쓰인다. 팀은 먼저 정상 흐름을 통해 전체 기능이 끝까지 연결되는지 확인하고, 그 다음 예외와 경계 조건을 보강한다. 하지만 일정이 부족할 때 정상 흐름만 검증하고 출시하는 위험한 선택이 발생하기도 한다. 특히 돈과 개인정보가 관련된 기능은 정상 흐름만으로는 절대 충분하지 않다. 경험 많은 실무자는 정상 흐름을 “출발점”으로 보고, 바로 이어서 실패 흐름과 복구 흐름을 설계한다.

### 예외 흐름(Exception Flow)

예외 흐름(Exception Flow)은 정상 흐름에서 벗어난 상황에서 시스템이 어떻게 동작해야 하는지를 설명하는 흐름이다. 사용자가 잘못된 값을 입력하거나, 권한이 없거나, 외부 API가 실패하거나, 재고가 사라지거나, 이미 처리된 요청을 다시 보내는 경우가 예외 흐름이 될 수 있다. 예외 흐름은 제품의 견고함을 결정한다. 정상 상황은 누구나 설계하지만, 예외 상황에서 사용자가 피해를 보지 않고 시스템 데이터가 망가지지 않도록 하는 것이 실제 품질이다.

예외 흐름은 오류 상태, 입력 검증, 상태 전이, 운영 정책, 테스트 케이스와 연결된다. 초보자는 예외를 “오류 메시지 표시”로만 생각한다. 하지만 예외 흐름에는 데이터 롤백, 재시도, 임시 저장, 고객센터 안내, 관리자 개입, 알림 발송, 로그 기록이 포함될 수 있다. 예를 들어 결제 승인은 성공했는데 주문 생성이 실패하면 단순히 “오류”라고 보여 주는 것으로 끝나지 않는다. 돈은 빠져나갔는지, 주문은 없는지, 자동 취소할지, 운영자가 확인할지, 사용자에게 어떤 안내를 할지 정해야 한다.

실무에서 예외 흐름을 모두 문서화하는 것은 어렵지만, 위험도가 큰 예외는 반드시 다뤄야 한다. 특히 외부 시스템 연동, 비동기 처리, 권한 변경, 대량 작업, 중복 클릭, 시간 제한, 재시도는 예외가 자주 생긴다. 경험 많은 실무자는 예외 흐름을 설계할 때 “부분적으로 성공한 상태”를 주의한다. 모든 것이 성공하거나 실패하면 단순하지만, 일부만 성공하면 정합성 문제가 생긴다. 이런 경우 보상 처리(Compensation), 재처리, 수동 조정 같은 운영 설계가 필요하다.

### 경계값 테스트(Boundary Value Testing)

경계값 테스트(Boundary Value Testing)는 입력값이나 조건의 경계 주변에서 오류가 자주 발생한다는 경험에 기반해 최소값, 최대값, 바로 아래 값, 바로 위 값을 집중적으로 검증하는 테스트 기법이다. 예를 들어 비밀번호가 최소 8자라면 7자, 8자, 9자를 테스트하고, 구매 수량이 1개 이상 99개 이하라면 0개, 1개, 99개, 100개를 테스트한다. 이 기법은 전통적인 소프트웨어 테스트 이론에서 널리 쓰이며, 실무에서도 매우 효과적이다.

경계값 테스트는 입력 검증, 정책, 데이터 타입, 테스트 케이스, 오류 메시지와 연결된다. 초보자는 대표값 하나만 넣어 보고 통과하면 충분하다고 생각하기 쉽다. 하지만 버그는 조건의 가운데보다 경계에서 더 자주 발생한다. 개발자가 `>`와 `>=`를 혼동하거나, 날짜 계산에서 마지막 날을 포함하지 않거나, 배열 길이 제한을 잘못 적용하는 경우가 많기 때문이다. 예를 들어 “가입 후 7일 이내 환불 가능”이라는 정책은 정확히 7일째 되는 순간을 포함하는지 제외하는지가 중요하다.

실무에서 경계값은 숫자만이 아니다. 날짜, 시간대, 문자열 길이, 파일 크기, 페이지 번호, 할인율, 재고 수량, 권한 단계, 상태 전이 횟수에도 경계가 있다. 특히 시간 경계는 어렵다. 자정, 월말, 윤년, 시간대 차이, 서머타임, 영업일 기준 같은 문제가 생긴다. 경험 많은 QA는 정책 문서를 읽을 때 경계 조건을 먼저 찾는다. 경계가 명확하지 않으면 테스트도 구현도 흔들리기 때문에, 기획 단계에서 경계값을 질문하는 것이 중요하다.

### 회귀 테스트(Regression Testing)

회귀 테스트(Regression Testing)는 기존에 잘 동작하던 기능이 새로운 변경으로 인해 깨지지 않았는지 확인하는 테스트다. 회귀라는 말은 품질이 이전보다 나빠지는 것을 의미한다. 소프트웨어는 서로 연결되어 있기 때문에 작은 수정이 예상치 못한 곳에 영향을 줄 수 있다. 예를 들어 쿠폰 계산 로직을 고쳤는데 장바구니 총액, 주문서, 환불 금액, 정산 보고서가 모두 영향을 받을 수 있다. 회귀 테스트는 이런 부작용을 잡기 위해 필요하다.

회귀 테스트는 자동화 테스트, 배포, 변경 영향도 분석, 핵심 시나리오, 버그 수정과 연결된다. 초보자는 수정한 부분만 다시 보면 된다고 생각하기 쉽다. 하지만 실무에서는 공통 컴포넌트, 공통 API, 공통 데이터 모델, 공통 권한 로직을 수정하면 여러 기능이 영향을 받는다. 회귀 테스트 범위를 정하려면 변경된 코드나 정책이 어디에 쓰이는지 알아야 한다. 그래서 개발자와 QA의 협업이 중요하다.

실무에서 모든 기능을 매번 수동으로 회귀 테스트하는 것은 비현실적이다. 그래서 핵심 흐름은 자동화 테스트로 만들고, 위험한 변경에는 집중 수동 테스트를 병행한다. 또한 회귀 테스트 세트는 제품이 성장할수록 관리가 필요하다. 오래된 기능, 거의 쓰지 않는 기능, 변경이 잦은 기능, 장애 비용이 큰 기능을 구분해야 한다. 경험 많은 팀은 배포 전 최소 회귀 테스트 목록을 가지고 있으며, 장애가 발생하면 그 사례를 회귀 테스트에 추가한다.

### 버그 리포트(Bug Report)

버그 리포트(Bug Report)는 발견된 결함을 개발자가 재현하고 수정할 수 있도록 정리한 보고서다. 좋은 버그 리포트에는 제목, 환경, 재현 단계, 기대 결과, 실제 결과, 발생 빈도, 첨부 증거, 심각도, 우선순위가 포함된다. 버그 리포트의 목적은 잘못을 지적하는 것이 아니라 문제를 빠르게 이해하고 해결하게 하는 것이다. 소프트웨어 개발에서 버그는 피할 수 없으므로, 버그를 어떻게 보고하고 추적하는지가 팀 품질에 큰 영향을 준다.

버그 리포트는 테스트 케이스, 이슈 트래커, 로그, 스크린샷, 우선순위 관리와 연결된다. 초보자는 “안 됩니다”, “이상합니다”, “저만 그런가요?”처럼 모호하게 보고하기 쉽다. 이런 보고는 개발자가 재현하기 어렵다. 좋은 보고는 “Chrome 125, macOS, 일반 회원 계정에서 주문 상세 화면의 환불 버튼을 누르면 500 오류가 발생한다. 같은 주문을 관리자 계정에서 처리하면 정상이다”처럼 조건과 차이를 드러낸다.

실무에서 버그의 심각도(Severity)와 우선순위(Priority)는 다르다. 심각도는 문제의 영향 크기이고, 우선순위는 언제 처리할지의 결정이다. 드물게 발생하지만 결제가 중복되는 버그는 심각도가 높고 우선순위도 높다. 반대로 오탈자는 심각도는 낮지만 출시 전 브랜드 이미지 때문에 우선 처리될 수도 있다. 경험 많은 QA와 PM은 버그 리포트에서 감정적 표현을 줄이고, 재현 가능한 사실과 사용자 영향도를 중심으로 설명한다.

## 6. 인프라 배포와 유지보수 운영

### 인프라(Infrastructure)

인프라(Infrastructure)는 소프트웨어가 실행되고 사용자에게 제공되기 위해 필요한 기반 환경 전체를 말한다. 서버, 네트워크, 데이터베이스, 저장소, 로드 밸런서, DNS, CDN, 컨테이너, 클라우드 계정, 모니터링 시스템, 로그 시스템이 모두 인프라에 포함될 수 있다. 원래 인프라는 도로, 전기, 수도 같은 사회 기반 시설을 뜻했고, IT에서는 애플리케이션이 돌아가기 위한 기반 시설을 의미하게 되었다. 사용자는 화면만 보지만, 그 화면 뒤에는 많은 인프라가 작동한다.

인프라는 배포, 확장성, 보안, 장애 대응, 비용, 성능과 연결된다. 초보자는 소프트웨어를 “코드만 있으면 실행되는 것”으로 생각하기 쉽다. 하지만 실제 서비스는 서버가 켜져 있어야 하고, 도메인이 연결되어야 하며, 데이터베이스가 접근 가능해야 하고, 인증서가 유효해야 하며, 로그와 모니터링이 있어야 한다. 개발자 컴퓨터에서 잘 돌아가는 앱도 운영 인프라에서는 환경변수, 네트워크 권한, 데이터베이스 버전, 파일 저장소 권한 때문에 실패할 수 있다.

실무에서 인프라 설계는 제품의 규모와 위험에 맞아야 한다. 작은 내부 도구에 대규모 분산 시스템을 도입하면 복잡성과 비용만 늘고, 반대로 많은 사용자가 쓰는 결제 서비스에 단일 서버와 수동 백업만 두면 위험하다. 클라우드가 보편화되면서 서버를 직접 구매하지 않고 AWS, GCP, Azure 같은 서비스를 쓰는 경우가 많지만, 클라우드를 쓴다고 운영 책임이 사라지는 것은 아니다. 경험 많은 운영자는 장애, 백업, 보안, 비용, 권한, 배포 자동화를 함께 본다.

### 배포(Deployment)

배포(Deployment)는 개발된 소프트웨어를 사용자가 접근할 수 있는 환경에 올려 실행시키는 과정이다. 과거에는 서버에 직접 파일을 복사하고 서비스를 재시작하는 방식이 많았지만, 현대에는 CI/CD 파이프라인, 컨테이너, 클라우드 배포, 무중단 배포 같은 방식이 널리 쓰인다. 배포는 개발이 끝났다는 선언이 아니라 실제 사용자가 기능을 쓰게 되는 위험한 전환점이다. 코드가 저장소에 합쳐졌다고 해서 배포된 것은 아니다.

배포는 빌드(Build), 릴리스(Release), 환경(Environment), 마이그레이션, 모니터링, 롤백과 연결된다. 초보자는 배포와 릴리스를 같은 말로 쓰기도 하지만, 구분하면 유용하다. 배포는 코드를 환경에 올리는 기술적 행위이고, 릴리스는 사용자에게 기능을 공개하는 제품적 행위다. 기능 플래그(Feature Flag)를 쓰면 코드는 이미 배포되어 있지만 특정 사용자에게만 공개하거나 아직 숨겨 둘 수 있다. 이 구분은 위험을 줄이는 데 중요하다.

실무에서 배포는 항상 실패 가능성을 고려해야 한다. 새 코드가 서버에서 실행되지 않을 수 있고, 데이터베이스 마이그레이션이 오래 걸릴 수 있으며, 외부 API 키가 운영 환경에 없을 수 있고, 캐시 때문에 예전 화면이 보일 수 있다. 그래서 배포 전 체크리스트, 배포 후 스모크 테스트, 모니터링 확인, 롤백 절차가 필요하다. 경험 많은 팀은 금요일 늦은 오후에 큰 배포를 피한다. 배포 후 문제가 생기면 대응할 사람이 부족하고, 주말 동안 장애가 길어질 수 있기 때문이다.

### 환경(Environment)

환경(Environment)은 소프트웨어가 실행되는 구분된 공간이나 조건을 말한다. 보통 로컬(Local), 개발(Development), 테스트(Test), 스테이징(Staging), 운영(Production) 환경으로 나뉜다. 로컬은 개발자 개인 컴퓨터, 개발 환경은 팀 내부 검증용 서버, 테스트 환경은 QA 검증용 서버, 스테이징은 운영과 거의 비슷한 사전 검증 환경, 운영은 실제 사용자가 쓰는 환경이다. 환경을 나누는 이유는 실험과 검증을 실제 사용자에게 영향을 주지 않고 하기 위해서다.

환경은 배포, 설정값(Configuration), 데이터베이스, API 키, 권한, 테스트와 연결된다. 초보자는 “내 컴퓨터에서는 되는데 운영에서는 왜 안 되지?”라는 상황을 자주 겪는다. 환경마다 운영체제, 라이브러리 버전, 네트워크 접근 권한, 환경변수, 데이터 양, 외부 서비스 연결이 다를 수 있기 때문이다. 특히 운영 환경의 실제 데이터는 개발 환경에 그대로 복사하면 개인정보 문제가 생길 수 있다. 그래서 환경별 데이터와 권한 관리가 중요하다.

실무에서 스테이징 환경은 운영과 최대한 비슷해야 의미가 있다. 하지만 비용이나 보안 때문에 완전히 같게 만들기는 어렵다. 외부 결제사는 테스트 모드와 운영 모드가 다르고, 이메일은 실제 발송을 막아야 할 수 있으며, 데이터 양도 다를 수 있다. 경험 많은 운영자는 환경 차이를 문서화하고, 환경변수와 비밀값을 안전하게 관리하며, 운영에서만 발생할 수 있는 문제를 모니터링으로 보완한다. 환경을 잘못 구분하면 테스트 데이터가 실제 고객에게 발송되거나, 운영 데이터가 개발 중 삭제되는 큰 사고가 날 수 있다.

### CI/CD(Continuous Integration and Continuous Delivery/Deployment)

CI/CD(Continuous Integration and Continuous Delivery 또는 Continuous Deployment)는 코드 변경을 자주 통합하고, 자동 빌드와 테스트를 거쳐, 배포 가능한 상태로 만들거나 자동 배포하는 개발 운영 방식이다. CI는 지속적 통합으로, 여러 개발자의 변경을 자주 합쳐 충돌과 오류를 빨리 발견하는 데 초점이 있다. CD는 문맥에 따라 지속적 전달 또는 지속적 배포를 의미한다. 지속적 전달은 언제든 배포 가능한 상태를 유지하는 것이고, 지속적 배포는 통과한 변경을 자동으로 운영에 배포하는 것이다.

CI/CD는 버전 관리, 테스트 자동화, 빌드, 배포, 롤백, 품질 게이트와 연결된다. 초보자는 CI/CD를 단순히 “자동 배포 도구”로 생각하기 쉽다. 그러나 핵심은 반복 가능한 품질 검증과 배포 절차를 자동화하는 것이다. 사람이 매번 수동으로 명령을 치면 실수할 수 있고, 어떤 테스트를 돌렸는지 누락될 수 있다. CI/CD 파이프라인은 코드가 바뀔 때마다 테스트, 린트, 빌드, 보안 검사, 이미지 생성, 배포 같은 단계를 일관되게 실행한다.

실무에서 CI/CD는 자동화 수준을 조직의 준비도에 맞춰야 한다. 테스트가 부족한 상태에서 운영 자동 배포를 하면 빠르게 장애를 배포할 수 있다. 반대로 모든 배포를 수동 승인으로만 처리하면 속도가 느려진다. 좋은 파이프라인은 실패하면 멈추고, 실패 이유를 명확히 보여 주며, 필요한 사람에게 알리고, 이전 버전으로 돌아갈 수 있는 정보를 남긴다. 경험 많은 팀은 배포 속도만 보지 않고 실패율, 복구 시간, 테스트 신뢰도, 승인 책임을 함께 본다.

### 롤백(Rollback)

롤백(Rollback)은 배포나 변경 후 문제가 발생했을 때 이전의 안정적인 상태로 되돌리는 작업이다. 데이터베이스 트랜잭션에서 실패 시 이전 상태로 되돌리는 개념에서 넓게 쓰이게 되었고, 운영에서는 코드 버전, 설정값, 데이터 마이그레이션, 기능 공개 상태를 되돌리는 의미로 사용된다. 롤백은 장애 대응의 핵심 안전장치다. 변경은 언제든 실패할 수 있으므로, 되돌릴 수 있는 계획이 있어야 한다.

롤백은 배포, 릴리스, 마이그레이션, 백업, 기능 플래그, 장애 대응과 연결된다. 초보자는 이전 코드로 다시 배포하면 항상 롤백이 된다고 생각하기 쉽다. 하지만 데이터베이스 스키마가 바뀌었거나, 새 코드가 데이터를 새로운 형식으로 저장했거나, 외부 시스템에 이미 요청을 보냈다면 단순 코드 롤백만으로 해결되지 않는다. 예를 들어 새 주문 상태값을 추가한 뒤 일부 주문이 그 상태로 저장되었는데 예전 코드가 그 상태값을 모르면 화면이 깨질 수 있다.

실무에서 좋은 롤백은 배포 전부터 계획된다. 어떤 변경은 즉시 롤백 가능하고, 어떤 변경은 롤백이 어렵다. 데이터 삭제, 대량 업데이트, 결제 처리, 외부 발송은 특히 되돌리기 어렵다. 그래서 위험한 변경은 단계적으로 배포하고, 기능 플래그로 끄거나, 데이터 마이그레이션을 되돌릴 수 있게 준비한다. 경험 많은 운영자는 배포 계획서에 “실패 감지 기준”, “롤백 판단자”, “롤백 절차”, “롤백 후 확인 항목”을 포함한다.

### 모니터링(Monitoring)

모니터링(Monitoring)은 운영 중인 시스템의 상태를 지속적으로 관찰해 성능, 오류, 사용량, 자원 상태, 비즈니스 지표를 확인하는 활동이다. 서버 CPU, 메모리, 디스크, 응답 시간, 오류율, API 호출 수, 결제 실패율, 가입 전환율 같은 지표를 수집하고 시각화한다. 모니터링은 시스템이 정상인지 사람이 계속 화면을 보고 확인하는 것이 아니라, 도구가 지표를 수집하고 이상 징후를 알려 주게 만드는 것이다.

모니터링은 로그, 알림(Alert), 관측 가능성(Observability), SLA, 장애 대응, 운영 대시보드와 연결된다. 초보자는 사용자가 신고하면 그때 알면 된다고 생각할 수 있다. 하지만 좋은 운영은 사용자가 느끼기 전에 문제를 발견하거나, 적어도 신고가 들어왔을 때 빠르게 원인을 찾을 수 있어야 한다. 예를 들어 서버는 살아 있지만 결제 API만 실패하고 있을 수 있고, 전체 오류율은 낮지만 특정 브라우저에서만 가입이 실패할 수 있다. 단순 서버 생존 여부만 보는 모니터링으로는 이런 문제를 놓친다.

실무에서 모니터링은 너무 많은 알림을 만들면 오히려 실패한다. 중요하지 않은 알림이 계속 울리면 사람들은 알림을 무시하게 된다. 좋은 모니터링은 사용자 영향이 있는 지표를 중심으로 경고를 만들고, 원인 분석에 필요한 세부 지표를 함께 제공한다. 또한 기술 지표와 비즈니스 지표를 연결해야 한다. 주문 생성 API 오류율이 올라가면 매출 손실로 이어질 수 있다. 경험 많은 운영자는 “시스템이 살아 있는가”보다 “사용자가 핵심 행동을 성공하고 있는가”를 본다.

### 로그(Logging)

로그(Logging)는 시스템에서 발생한 사건, 요청, 오류, 상태 변화를 시간순으로 기록하는 것이다. 개발자는 로그를 통해 문제가 발생했을 때 어떤 요청이 들어왔고, 어떤 코드 경로를 지나갔으며, 어떤 오류가 났는지 확인한다. 로그는 항해 일지처럼 나중에 과거를 추적하기 위한 기록이다. 일반 로그, 오류 로그, 접근 로그, 감사 로그, 보안 로그 등 목적에 따라 종류가 나뉜다.

로그는 모니터링, 디버깅, 감사, 보안, 개인정보 보호와 연결된다. 초보자는 로그를 많이 남기면 좋다고 생각할 수 있지만, 로그는 양과 품질의 균형이 중요하다. 너무 적으면 원인을 찾을 수 없고, 너무 많으면 비용이 커지고 중요한 신호가 묻힌다. 또한 민감정보를 로그에 남기면 큰 보안 문제가 된다. 비밀번호, 인증 토큰, 카드번호, 주민등록번호, 개인 식별 정보는 로그에 그대로 남기면 안 된다.

실무에서 좋은 로그는 맥락을 담는다. 단순히 “오류 발생”이라고 남기는 것보다 요청 ID, 사용자 ID, 엔드포인트, 처리 단계, 외부 응답 코드, 소요 시간, 실패 원인을 함께 남기면 추적이 쉽다. 분산 시스템에서는 하나의 사용자 요청이 여러 서버를 거치므로 상관관계 ID(Correlation ID)가 중요하다. 경험 많은 개발자는 로그를 나중에 자신이 새벽에 장애 대응할 때 읽는다고 상상하며 남긴다. 즉, 로그는 현재 개발자를 위한 메모가 아니라 미래의 조사자를 위한 증거다.

### 알림(Alert)

알림(Alert)은 시스템에 이상 징후나 중요한 이벤트가 발생했을 때 담당자에게 즉시 알려 주는 장치다. 이메일, 슬랙, 문자, 전화, 호출 시스템 등을 통해 전달될 수 있다. 알림은 모니터링 지표가 기준치를 넘었을 때 발생하는 경우가 많지만, 단순 오류뿐 아니라 배포 완료, 백업 실패, 인증서 만료, 디스크 용량 부족, 결제 실패율 증가 같은 운영 이벤트에도 쓰인다. 알림의 목적은 사람이 필요한 시간에 개입하게 하는 것이다.

알림은 모니터링, 장애 대응, 온콜(On-call), SLA, 운영 정책과 연결된다. 초보자는 모든 오류를 알림으로 보내면 안전하다고 생각할 수 있다. 그러나 알림이 너무 많으면 알림 피로(Alert Fatigue)가 생긴다. 담당자는 중요한 알림을 놓치거나, 밤새 불필요한 알림에 시달려 실제 장애 대응 능력이 떨어진다. 좋은 알림은 조치가 필요한 문제에 집중해야 한다. “오류가 한 번 발생했다”보다 “5분 동안 결제 실패율이 평소보다 3배 높고 사용자 영향이 있다”가 더 유용하다.

실무에서 알림에는 심각도와 대응 절차가 필요하다. 모든 알림이 즉시 깨워야 하는 것은 아니다. 서비스 전체 중단, 결제 불가, 데이터 손실 위험은 즉시 대응이 필요하지만, 낮은 우선순위의 배치 실패는 업무 시간에 처리해도 될 수 있다. 알림 메시지에는 무엇이 문제인지, 어느 서비스인지, 언제부터인지, 관련 대시보드와 로그 링크는 무엇인지, 첫 대응 방법은 무엇인지가 들어가야 한다. 경험 많은 운영자는 알림을 만들 때 “이 알림을 받은 사람이 무엇을 해야 하는가”를 먼저 묻는다.

### 장애(Incident)

장애(Incident)는 서비스가 정상적으로 제공되지 않거나 사용자에게 의미 있는 피해를 주는 사건이다. 서버 다운, 결제 실패, 로그인 불가, 데이터 손상, 보안 침해, 심각한 성능 저하, 잘못된 알림 대량 발송 등이 장애가 될 수 있다. 장애라는 말은 운영과 보안에서 널리 쓰이며, 단순한 버그보다 사용자 영향과 긴급 대응이 필요한 사건을 의미한다. 모든 버그가 장애는 아니지만, 사용자에게 큰 영향을 주는 버그는 장애로 관리해야 한다.

장애는 모니터링, 알림, 온콜, 장애 대응 절차, 사후 분석(Postmortem), SLA와 연결된다. 초보자는 장애가 나면 원인부터 찾으려 하지만, 실무에서는 먼저 사용자 영향을 줄이는 것이 중요하다. 원인 분석은 필요하지만, 서비스 복구가 더 급한 경우가 많다. 예를 들어 새 배포 후 로그인 불가가 발생했다면 자세한 코드 원인을 찾기 전에 롤백하거나 기능을 끄는 것이 우선일 수 있다. 장애 대응은 탐정 놀이가 아니라 피해 최소화 활동이다.

실무에서 장애 대응에는 역할 분담이 필요하다. 누가 지휘할지, 누가 기술 조사를 할지, 누가 고객과 내부 이해관계자에게 공지할지, 누가 로그를 수집할지 정해야 한다. 장애 중에는 혼란이 크므로 기록도 중요하다. 언제 감지했고, 어떤 조치를 했고, 어떤 지표가 회복되었는지 남겨야 사후 분석이 가능하다. 경험 많은 팀은 장애를 숨기기보다 학습 기회로 삼는다. 사후 분석은 개인 탓을 하기보다 시스템과 절차가 어떻게 개선될 수 있는지 찾는 과정이어야 한다.

### SLA(Service Level Agreement)와 SLO(Service Level Objective)

SLA(Service Level Agreement)는 서비스 제공자가 고객이나 내부 이해관계자에게 약속하는 서비스 수준 계약이다. 예를 들어 월간 가용성 99.9퍼센트, 고객 문의 응답 시간, 장애 복구 시간 같은 약속이 포함될 수 있다. SLO(Service Level Objective)는 그 약속을 달성하기 위한 내부 목표 수준이다. SLA는 계약적 성격이 강하고, SLO는 운영 목표 성격이 강하다. 이 개념들은 대규모 서비스 운영과 클라우드 서비스에서 특히 중요해졌다.

SLA와 SLO는 모니터링, 알림, 장애 대응, 우선순위, 비용과 연결된다. 초보자는 100퍼센트 가용성을 목표로 하면 좋다고 생각할 수 있다. 하지만 완벽한 무중단은 매우 비싸고 현실적으로 어렵다. 가용성 목표가 높아질수록 인프라 이중화, 자동 복구, 24시간 대응, 테스트, 운영 인력이 더 필요하다. 그래서 서비스의 중요도와 사용자 기대에 맞는 목표를 정해야 한다. 내부 관리자 도구와 금융 결제 시스템은 같은 수준의 가용성을 요구하지 않는다.

실무에서 SLA는 제품과 운영의 약속을 수치로 바꾸는 일이다. 하지만 수치만 정하면 끝이 아니다. 어떻게 측정할지, 어떤 장애를 포함할지, 점검 시간은 제외할지, 보상은 어떻게 할지 정해야 한다. SLO를 잘 운영하면 알림 기준도 더 현실적으로 만들 수 있다. 예를 들어 오류 예산(Error Budget)이라는 개념을 사용해 허용 가능한 실패 범위 안에서는 개발 속도를 유지하고, 실패가 많아지면 안정화 작업을 우선할 수 있다. 경험 많은 운영자는 SLA를 마케팅 문구가 아니라 실제 운영 능력과 연결해 본다.

### 백업(Backup)과 복구(Restore)

백업(Backup)은 데이터나 시스템 상태를 별도로 저장해 두는 것이고, 복구(Restore)는 문제가 발생했을 때 그 백업을 이용해 필요한 상태로 되돌리는 것이다. 백업은 데이터 손실, 실수로 인한 삭제, 시스템 장애, 랜섬웨어, 마이그레이션 실패에 대비하기 위한 기본 운영 장치다. 하지만 백업이 있다는 말만으로 안전한 것은 아니다. 실제로 복구할 수 있어야 의미가 있다. 많은 조직이 백업은 해 두었지만 복구 테스트를 하지 않아 위기 때 실패한다.

백업과 복구는 데이터베이스, 파일 저장소, 재해 복구(Disaster Recovery), RPO, RTO, 보안과 연결된다. RPO(Recovery Point Objective)는 어느 시점까지의 데이터 손실을 감수할 수 있는지, RTO(Recovery Time Objective)는 얼마나 빨리 복구해야 하는지를 뜻한다. 초보자는 매일 백업하면 충분하다고 생각할 수 있지만, 결제나 주문 시스템에서 하루치 데이터 손실은 치명적일 수 있다. 반대로 중요도가 낮은 로그 데이터는 더 느슨한 백업 정책이 가능하다.

실무에서 백업은 암호화, 접근 권한, 보관 기간, 지역 분산, 복구 절차까지 포함해야 한다. 백업 파일에 개인정보가 들어 있다면 그것도 보호 대상이다. 또한 운영 데이터베이스만 백업하고 업로드 파일이나 설정값을 빼먹으면 완전 복구가 어렵다. 경험 많은 운영자는 정기적으로 복구 훈련을 한다. 백업은 존재 여부가 아니라 “정해진 시간 안에 실제로 복구 가능한가”로 평가해야 한다.

### 마이그레이션(Migration)

마이그레이션(Migration)은 데이터, 스키마, 시스템, 인프라를 한 상태에서 다른 상태로 옮기는 작업이다. 데이터베이스 컬럼 추가, 테이블 분리, 기존 데이터 변환, 서버 이전, 클라우드 전환, 결제사 교체, 도메인 변경이 모두 마이그레이션이 될 수 있다. 마이그레이션은 단순 복사가 아니라 기존 사용자의 데이터와 서비스를 유지하면서 구조를 바꾸는 작업이기 때문에 위험하다. 특히 운영 중인 서비스에서는 사용자가 계속 데이터를 만들고 있는 동안 변경해야 할 수 있다.

마이그레이션은 스키마 변경, 배포, 롤백, 백업, 데이터 정합성, 다운타임과 연결된다. 초보자는 마이그레이션을 “한 번 스크립트 돌리면 끝”으로 생각하기 쉽다. 그러나 실무에서는 사전 백업, 테스트 환경 리허설, 예상 소요 시간, 중간 실패 시 처리, 데이터 검증, 롤백 가능성, 사용자 공지, 배포 순서를 고려해야 한다. 대량 데이터 마이그레이션은 시간이 오래 걸릴 수 있고, 실행 중 서비스 성능에 영향을 줄 수 있다.

실무에서 안전한 마이그레이션은 단계적으로 진행된다. 예를 들어 새 컬럼을 먼저 추가하고, 새 코드가 양쪽 컬럼을 모두 처리하게 만들고, 기존 데이터를 천천히 채우고, 검증 후 예전 컬럼을 제거하는 식이다. 이것을 확장과 수축(Expand and Contract) 방식이라고 부르기도 한다. 경험 많은 개발자는 마이그레이션에서 가장 무서운 것이 “되돌릴 수 없는 데이터 변경”임을 안다. 그래서 실행 전 샘플 검증, 실행 중 진행률 확인, 실행 후 카운트와 무결성 검사를 준비한다.

## 맺음말

소프트웨어 제품을 만든다는 것은 화면 몇 장을 그리고 개발자에게 전달하는 일이 아니다. 제품은 정보 구조 위에서 사용자가 길을 찾고, 화면 명세를 통해 행동이 정의되며, 비즈니스 정책으로 판단 기준이 정해지고, 데이터 모델과 API로 시스템 내부의 약속이 만들어지며, QA 테스트로 품질이 검증되고, 인프라 운영으로 실제 사용자에게 안정적으로 제공된다. 이 문서에서 설명한 용어들은 각각 따로 존재하는 단어가 아니라 하나의 생산 체계 안에서 서로 연결된 실무 언어다.

처음 배우는 사람에게 가장 중요한 태도는 용어를 아는 척하지 않는 것이다. 회의에서 “정책이 필요합니다”, “상태값을 정의해야 합니다”, “예외 케이스가 빠졌습니다”, “API 계약이 불명확합니다”, “롤백 계획이 있나요”라는 말을 들었을 때, 그 단어가 지시하는 실제 질문을 이해해야 한다. 제품 문서는 아름다운 문장이 아니라 팀이 같은 것을 만들게 하는 합의 장치다. 따라서 좋은 문서는 정의, 조건, 예외, 책임, 검증 기준을 드러낸다. 초보자는 처음부터 완벽한 시스템 설계자가 될 필요는 없지만, 각 용어가 어떤 문제를 해결하기 위해 존재하는지 알면 전문가와 대화할 수 있고, 빠진 질문을 발견할 수 있으며, PRD를 실제 생산 가능한 설계 산출물로 확장할 수 있다.
